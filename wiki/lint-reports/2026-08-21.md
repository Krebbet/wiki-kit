# Lint Report — 2026-08-21

Scope: full wiki (126 files — 112 top-level pages, 13 conflict pages, this and the prior lint report),
audited via 9 parallel subagents (8 content batches of ~13 pages each, one dedicated conflicts-page batch),
plus direct mechanical checks (link-graph cross-reference, capture-fidelity audit, un-ingested-source scan)
run by the coordinator. This is the second lint run; the first (`2026-08-19.md`) covered a much smaller wiki
before jobs 8–17 landed. Since then the wiki grew from roughly 40 pages to 126, across ten additional research
jobs (8, 9, 10, 12, 13, 11, 14, 15, 16, 17), including the wiki's first case-study pages.

## Orphan Pages

**None.** Every one of the 121 content pages (excluding `index.md`, `CLAUDE.md`, `revisions.md`, `log.md`,
and the lint-reports directory, which are meta files by design) has at least one inbound `[[wiki-link]]`
from another content page. Verified mechanically by extracting every outbound link from every file and
cross-referencing against the full page-file list.

## Broken Links

**None.** Every `[[wiki-link]]` reference in the wiki resolves to a real page. (Three apparent hits —
`[[page-name]]`, `[[wiki-link]]`, `[[wiki-links]]` — are literal example syntax inside `CLAUDE.md`'s own
documentation and two historical log/lint-report entries describing that documentation, not real dangling
links.)

## Missing Cross-References

No systematic gaps surfaced. Cross-linking discipline across the 100+ new pages from this session's six jobs
is unusually strong — most pages carry 3–15 `## Related` links, and several subagents independently flagged
specific pages (`credible-commitment.md`, `developmental-state-and-embedded-autonomy.md`,
`regulatory-capture.md`, `schooling-and-institutional-quality.md`, `world-management-survey.md`) as
exemplary reference points for house style.

## Format Issues

- **`wiki/conflicts/iron-law-vs-design-principles.md`** — missing both a `## Source` header (sources are
  given inline per-position instead of consolidated) and a `## Related` header (the file ends with a bare
  "See [[iron-law-of-oligarchy]] and [[polycentric-governance]]." with no section heading). The only file of
  126 that fails the mandated page format. Also: its H1 title ("Michels vs. Ostrom on Whether Participatory
  Design Prevents Oligarchic Drift") doesn't match its filename slug, unlike every other conflict page in the
  directory, which makes it harder to find by name. Created 2026-08-21 (job 11); reads like it received a
  lighter finishing pass than its 12 siblings. **Fixed this run** — see Domain-Specific Issues.

## Conflicts

No factual contradictions found between pages. One structural issue: **seven of the wiki's thirteen open
conflict pages have not been touched since 2026-08-19**, despite ten further research jobs running since —
`decay-as-real-vs-decay-as-overstated.md`, `efficiency-of-institutions-north-vs-williamson.md`,
`functional-vs-rent-seeking-growth.md`, `homogenisation-vs-loose-coupling.md`,
`parkinson-vs-knowledge-hierarchy-growth.md`, `presumed-vs-measured-public-sector-risk-aversion.md`, and
`rival-models-of-institutional-change.md`. None was found to contain content a later job has since
contradicted — but `decay-as-real-vs-decay-as-overstated.md` in particular is the wiki's stated central-thesis
conflict, and its own sibling page `downs-vs-merton-on-age-dependence.md` (created 2026-08-20, extending the
same dispute) is not cross-referenced back into the parent file. This is a genuine candidate for a follow-up
pass, not an urgent fix — flagged here as a coverage gap rather than corrected this run, since resolving it
properly requires re-reading each conflict against ten subsequent jobs' worth of new evidence, which is
follow-up-research-scale work, not a mechanical lint fix.

## Coverage Gaps

**Un-ingested raw sources: none.** Every one of the 128 files under `raw/research/*/` is cited in at least
one wiki page's `## Source` section — mechanically verified. No auto-ingest step is needed this run.

**Conceptual gaps** (need human judgement, not auto-actioned):
- **Fama & Jensen (1983)** — the canonical decision-rights decomposition (initiation/ratification/
  implementation/monitoring) — remains uncaptured. Both `separation-of-ownership-and-control.md` and
  `decision-rights-and-authority-theory-of-the-firm.md` flag this explicitly (tracked at Q113) rather than
  writing the taxonomy from memory. Still open.
- **A second Gilens & Page rebuttal (Enns)** failed capture on retry and is not held; the wiki's ideological-
  balance minimum is met by Bashir alone, and `whose-preferences-predict-policy-gilens-page.md` says so
  directly. Worth a manual-acquisition attempt if a second rebuttal is wanted.
- **`reference-sources.md`'s four "deliberate coverage gaps"** — Chinese-language, Nordic in-country,
  Japanese/Korean, and Latin American/African scholarship — are only partially addressed. Job 14
  (`comparative-governing-philosophies`) captured English-language sources *about* China, Japan, Korea and the
  Nordics (Brødsgaard, Xu, Colignon & Usui, Han/KDI, Kettunen), but these are still outsider/English-language
  analysis, not in-country-language primary scholarship — the gap as originally framed remains open.
- **Weber's "Bureaucracy" (Economy and Society ch. VIII)** — still uncaptured (scanned PDF, no text layer);
  job 8's bureaucracy coverage runs on Evans & Rauch and Rauch instead. Long-standing, already documented.

## Stale Content

**The retired `[framework]` evidence-tier token is still present in 10 files**, despite `CLAUDE.md`
recording it as normalised to `[model]` wiki-wide on 2026-08-20. The 2026-08-20 sweep evidently didn't reach
every page (some of these predate that date; a few are from this session's jobs and were simply missed):

`institutional-isomorphism.md` (3×), `institutional-myths-and-decoupling.md` (2×),
`motivation-crowding.md` (2×), `transaction-costs.md` (~9×, one of the wiki's oldest and most-cited pages),
`gradual-institutional-change.md` (3×), `incentives-under-multiple-principals.md` (1×),
`path-dependence-and-increasing-returns.md` (3×), `punctuated-equilibrium-and-the-state.md` (3×),
`polycentric-governance.md` (1×).

**Fixed this run** — see Domain-Specific Issues.

## Domain-Specific Issues

**Framework operationalisation.** All axes proposed anywhere in the wiki — including ones cited in passing
on topic pages, not just the register — state a measurement procedure or are explicitly marked `rejected`
with a stated readmission condition. No silent unmeasurable axes found. A handful of pages (`decision-rights-
and-authority-theory-of-the-firm.md`'s "payoff rights vs. decision rights," `organizational-economics-of-the-
state.md`'s departmental-architecture tradeoffs, `maghribi-traders-reputation-mechanism.md`'s D137 network-
density threshold) name a candidate axis and openly state no measurement procedure exists yet — compliant per
the check's own rule, since the gap is stated rather than hidden.

**Evidence-tier labelling.** Beyond the retired `[framework]` token above, a smaller set of pages uses
genuinely non-standard base tokens outside the fixed `[empirical]`/`[model]`/`[wiki synthesis]` vocabulary —
distinct from the widespread and *acceptable* practice of appending an attribution suffix to a correct base
token (`[model — Coase 1937]`, `[empirical, second-hand]`), which several batches independently confirmed is
established, compliant house style. The real violations:

- `isomorphic-mimicry-and-capability-traps.md` — entire body uses `[b — ...]`/`[c — wiki synthesis]` instead
  of the fixed tokens (the lettered form is permitted only in prose and Evidence-tier-note headers, never as
  the inline per-claim marker).
- `experimentalist-governance.md`, `ambidexterity-as-a-dynamic-capability.md`,
  `political-economy-of-public-sector-reform.md`, `punctuated-equilibrium-and-policy-change.md`,
  `personnel-economics-of-the-state.md` (pervasive) — same lettered-inline-marker pattern, mixed with correct
  usage elsewhere on the same page.
- `bureaucratic-growth-and-parkinsons-law.md`, `blame-avoidance-and-negativity-bias.md`,
  `exploration-exploitation-incentive-contracts.md` — use the word "assertion" as a tier marker, which isn't
  in the fixed vocabulary at all.
- `nation-building-and-education.md` (`[model/qualitative]`), `neo-weberian-state-and-administrative-
  traditions.md` (`[empirical, cited]`) — minor compound-token drift.

**Fixed this run** — see below.

**Case/theory linkage — now applicable, and asymmetric.** The wiki has three case-study pages
(`fda-case-profile.md`, `meta-case-profile.md`, `meta-oversight-board-case-profile.md`, all from job 15). All
three correctly link outward to the framework/mechanism pages whose axes they apply
(`dimensions-of-institutional-variation`, `credible-commitment`, `formal-and-real-authority`,
`corporate-ownership-and-control-around-the-world`, `veto-players-and-policy-stability`,
`worldwide-bureaucracy-indicators`, `reorganisation-base-rate`). But **none of those six framework pages
link back** to the case that now applies them — mechanically confirmed by grep. This is expected for the
pages written before job 15 existed, but is now a real, fixable gap. **Fixed this run** — see below.

**Case-study tagging.** All three case-study pages state sector, scale, and country in their opening
paragraph (FDA: public, federal agency, US; Meta: public company/private-sector, ~78,865 employees, US; the
Oversight Board: a Meta-created sub-institution). Age is present for Meta (founded 2004, stated implicitly via
the FY2025/2026 filings) but not stated as a number on any of the three pages. Minor — worth adding on a
future pass, not fixed this run (would require re-verifying exact founding dates against source documents
rather than a mechanical edit).

**Scope-condition creep.** One soft instance: `curriculum-and-ideology.md`'s closing clause ("the direction
of the shift is whatever the content-setter wants") generalises beyond its single-country natural experiment
without a local scope caveat at that specific sentence, though the rest of the page is otherwise carefully
China-scoped. Not fixed this run — low severity, and any fix risks over-editing a page that is otherwise
exemplary.

**Ideological balance.** No page found citing only one camp on a contested question without disclosure.
`whose-preferences-predict-policy-gilens-page.md` self-discloses its balance rests on one rebuttal (Bashir)
after a second (Enns) failed capture — already tracked as a coverage gap above, not a silent violation.

**Thesis drift.** None found. Every page touching the power/institutions/economy triad or the collapse-and-
renewal book thesis treats it as the working hypothesis it is, not an established finding — including the
newest pages from this session (`schooling-and-institutional-quality.md`'s rewrite is explicit that "if the
claim holds" is doing real work in its own inference section).

## Capture Fidelity (raw/)

**Clean across all 17 raw research topic directories** — `audit_captures` reports 0 issues (broken image
refs, missing PDF pairs, thin extractions, image-path collisions) for every job from `foundations-nie`
through `schooling-norms-and-institutional-formation`.

**Caveat carried from `master_notes.md`, not re-litigated here**: `audit_captures`'s own checks are known to
be incomplete — it gates its two most valuable checks behind a `pdfs/` directory `capture_pdf` never creates,
and it cannot detect a bot-wall page captured as if it were real content (exactly the SEC EDGAR failure found
and manually fixed during job 15, logged 2026-08-21). A clean `audit_captures` run is necessary, not
sufficient, evidence of capture fidelity. No further silent failures were found by manual spot-check (word
counts) across this session's six jobs, but the tool's blind spot is structural, not resolved.

## Trend Radar

Current watched-source set (`reference-sources.md`), all still `probation` — no weekly sweep has run yet
(first one scheduled 2026-08-25):

- Working papers: NBER, SSRN (Political Economy/Public Choice, Corporate Governance), arXiv econ.GN
- Journals: JEP, Journal of Institutional Economics, *Governance*, APSR/AJPS, ASQ/Organization Science, JEL
- Data/indices: V-Dem, WGI, QoG, OECD Public Governance, IMF/World Bank flagships, national audit bodies
- Blogs (discovery only, cap 2/sweep): VoxEU/CEPR, Broadstreet, Marginal Revolution, Works in Progress,
  Statecraft, Institute for Government (UK), Niskanen Center, Mercatus Center
- Lectures: YouTube author talks, Ostrom Workshop recordings

## Radar Evolution

**Additions this run**, based on venues this session's six jobs actually cited:

- **Journal of Political Economy** — added to Journals. Cantoni et al.'s curriculum-and-ideology paper
  (this session's cleanest natural-experiment evidence) was published here; not previously on the radar
  despite being a top-tier general-econ venue this wiki will keep needing.
- **Annual Review of Economics** — added to Journals. Acemoglu, Gallego & Robinson's institutions-vs-human-
  capital rebuttal ran here; a survey-heavy venue in the same register as JEL.
- **New section: Primary institutional documents.** Job 15 (`institution-case-profiles`) is a new kind of
  work for this wiki — scoring real institutions from their own primary documents rather than literature —
  and it needs its own watched-source category: SEC EDGAR filings (10-Ks, proxy statements), federal-agency
  Inspector General reports (oig.hhs.gov and peer state/national audit bodies), and company investor-relations
  document hosts (the pattern behind Meta's and the Oversight Board's charter/bylaws captures). **Carries a
  standing caveat**: SEC EDGAR specifically requires a *declared*, non-browser User-Agent — the wiki's capture
  tool's browser-spoofing default silently returns a bot-block page there (found and logged to
  `master_notes.md` 2026-08-21) — any future SEC EDGAR capture needs the manual workaround documented in
  `capture-manifests.md`'s job 15 entry until the tool itself is fixed.

**No removals** — every existing entry is still `probation`, so none has had the three-consecutive-dry-sweeps
chance to go stale yet.

---

## Fixes applied this run

Per the skill's process, mechanical, low-risk fixes were applied directly rather than merely reported:

1. **`wiki/conflicts/iron-law-vs-design-principles.md`** — added a consolidated `## Source` section and a
   `## Related` header (previously a bare unheaded link line).
2. **Retired `[framework]` token → `[model]`**, mechanically, in all 10 files listed under Stale Content.
3. **Six framework pages** (`worldwide-bureaucracy-indicators.md`, `reorganisation-base-rate.md`,
   `corporate-ownership-and-control-around-the-world.md`, `credible-commitment.md`,
   `formal-and-real-authority.md`, `veto-players-and-policy-stability.md`) — added a `## Related` link to the
   case-study page that now applies their axis, closing the case/theory-linkage gap.

**Not fixed this run** (offered to the user separately): the non-standard lettered-inline evidence-tier
markers (5 files) and the `[assertion]`/compound-token drift (5 files) — these need per-instance judgement
about which fixed token each marked claim actually is, rather than a safe mechanical find-replace, so they
are listed for the user's call rather than auto-corrected.

## Harvest checkpoint

Two things worth promoting to `master_notes.md` (`Scope: kit`, `Status: open`):

1. **A `[framework]`-style token retirement needs a wiki-wide grep, not a spot pass.** The 2026-08-20
  normalisation missed 10 of the wiki's ~126 pages — worth a standing lint check item (already effectively
  added this run: "grep for any retired evidence-tier token across the whole wiki, not just recently-touched
  pages," which the domain-lint-checks slot should probably state explicitly for future runs of this kit.
2. **Case/theory linkage is directional and needs an explicit backward-check.** The lint skill's own check 13
  asks whether case pages link to frameworks and vice versa, but it's easy for a case-study job to correctly
  link forward to the frameworks it applies while every pre-existing framework page — written before the case
  existed — has no way to know to link back. Worth stating in the skill itself that check 13 should include a
  mechanical "does the framework page's own Related section, post-dating the case, list it" pass, not just a
  read of the case page.
