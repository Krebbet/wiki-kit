# Lint Report — 2026-04-23

Lint run covering all 46 non-empty wiki pages and 64 captured raw sources across 8 research topic directories.

**Headline:** Wiki is structurally healthy. 0 real broken links, 0 orphan pages, 0 format issues, 0 thin captures, 1 capture-fidelity collision (benign). Two domain-level attribution flags and four stale-source pages identified. Six 0-byte stray stub files at `wiki/` root (Obsidian auto-creation) flagged for cleanup.

---

## Orphan Pages

**None.** Every non-exempt page in the wiki has at least one inbound `[[wiki-link]]`. Exemptions: `index.md`, `CLAUDE.md`, `revisions.md`, `log.md`, `research-queue.md`.

Inbound-link distribution sanity check: lowest-referenced content pages are `boycott-apps` (2), `time-banks-lets` (2), `open-food-network` (5), `data-disruption-strategy-map` (8), `algorithmwatch` (9), `auto-enrollment-opt-out` (9). All are recently added pages in thin content areas — visible but lightly networked. No action needed.

---

## Broken Links

**None real.** Automated extraction surfaced two matches, both intentional:

- `[[page-name]]` in `wiki/CLAUDE.md` — format-convention example in the operating manual. Not a real link.
- `[[wiki-link]]` in `wiki/CLAUDE.md` — same. Not a real link.

(Link extractor was fixed mid-run to handle escaped pipes `\|` inside Obsidian table cells, which had produced false-positive broken links on first pass.)

---

## Missing Cross-References

Spot-checked; no systematic gaps found. All tool/org pages link to at least one mechanism page (see Domain Check 10b). All industry pages link to the overview hub. No first-mention instances of a concept appearing without a `[[wiki-link]]` on pages where the concept has a dedicated page.

---

## Format Issues

**None.** Every content page has:
- `# Title` at the top
- `## Source` section
- `## Related` section

All 51 files checked; none missing required sections.

---

## Conflicts

No new conflicts flagged. One prior conflict — CCA's "no comparable opt-out default outside electricity" claim is imprecise in light of `auto-enrollment-opt-out` (US auto-IRAs as a second precedent) — was noted in the 2026-04-22 research-run log as resolved through new evidence, and the readout pages now cross-reference both precedents correctly.

`wiki/conflicts/` directory exists but is currently empty — no open conflict documents.

---

## Coverage Gaps

### Conceptual gaps (for user judgement)

- **`surveillance-pricing-retail` has thinner counter-power coverage than other industries.** `rental-housing-algorithmic-pricing` is addressed by `open-tenant-screening`, `collective-bargaining-for-data`, DOJ enforcement in `regulatory-responses`, and Yale TAP material on the page itself. `consumer-facing-dynamic-pricing` is addressed by `keepa`, `drivers-cooperative`, `drivers-seat-cooperative`. But `surveillance-pricing-retail` has only `markup-citizen-browser` (the observatory layer) as a direct counter-power tool. The FTC 6(b) 8-vendor target list (Mastercard, Revionics, Bloomreach, JPMC, Task, PROS, Accenture, McKinsey) is named but no page surfaces which retailers actually deploy their stacks — the `data-disruption-strategy-map` flags this as Open Question 2.
- **Data-broker layer under-covered.** Acxiom, Experian, Epsilon are mentioned briefly in `surveillance-pricing-retail` and `data-disruption-strategy-map` (L5) but no dedicated page exists. `data-disruption-strategy-map` flags the research-queue entry.

### Un-ingested raw sources

**None.** All 64 captured source files under `raw/research/*/` are cited on at least one wiki page via a `## Source` section. The only uncaptured `raw/*.md` is `raw/README.md` itself (directory-level readme, not a source).

---

## Stale Content

Four pages where source dates suggest a freshness concern on fast-moving topics:

- **`wiki/tools/markup-citizen-browser.md`** — both captured sources dated October 2020 and January 2021. Project is live (continues publishing as of 2026 per page narrative), but all *captured* source documents are ~5 years old. Recommend: capture a recent Markup publication or project-page snapshot.
- **`wiki/organizations/algorithmwatch.md`** — source is their `/what-we-do/` page but no explicit capture date in the source metadata; cannot verify recency. Recommend: add explicit `captured_on` to metadata or refresh-and-recapture.
- **`wiki/organizations/drivers-seat-cooperative.md`** — most recent explicit source is Ada Lovelace 2021; body cites December 2022 statistics. The org itself is known to have sunset (driversseat.co is a Squarespace expired-site stub per master_notes entry); this page is historical by nature, but the "has since sunset" framing would benefit from a recent citation.
- **`wiki/organizations/open-food-network.md`** — source is `openfoodnetwork.org/software-platform/` page with no date confirmed. Recommend: verify recency.

Other tool/org pages all have ≥1 source dated 2024 or later. No tool/org page references deprecated practices or unresolved TODOs.

---

## Domain-Specific Issues (checks 10a–10e)

### 10a. Source metadata — **PASS**

Every source file cited in the wiki has full origin / intended audience / purpose / trust-tag metadata on at least one wiki page. 73 distinct raw-source paths audited; zero missing.

### 10b. Conceptual cross-links — **PASS**

Every file in `wiki/tools/` and `wiki/organizations/` links to at least one mechanism page from the accepted list. Spot-check of 21 files (9 tools + 12 organizations) confirmed by subagent review. Zero gaps.

### 10c. Industry ↔ counter-power balance — **FLAGGED**

- **`surveillance-pricing-retail`**: only `markup-citizen-browser` addresses it as a direct counter-power tool. Thinner than other industry pages. Covered in Coverage Gaps above.
- No counter-power tactic pages are without industry targets.

### 10d. Claim attribution — **TWO FLAGS**

Two specific-claims rely on Wikipedia or a secondary summary rather than the primary source:

- **`mechanisms/algorithmic-collusion.md`** — the claim that Assad et al. (2024) found a **"28% margin increase"** from algorithmic pricing in German gasoline markets is cited via the arXiv 2504.16592 survey, which cites Assad et al. The primary paper is *"Algorithmic Pricing Facilitates Tacit Collusion"* by Assad, Clark, Ershov & Xu, published in the *Journal of Political Economy* (or recent working paper). The wiki should capture the primary paper or add a footnote-trail acknowledgment for the specific margin figure.
- **`organizations/park-slope-food-coop.md`** — the **66.43% / 66.6% June 2025 vote** on a specific governance proposal is cited only via Wikipedia with no footnote-trail annotation for that particular claim. A foodcoop.com official minutes source or contemporary press coverage would strengthen this.

### 10e. Stale tech — **FLAGGED**

See "Stale Content" above. Four pages flagged: `markup-citizen-browser`, `algorithmwatch`, `drivers-seat-cooperative`, `open-food-network`.

---

## Capture Fidelity (raw/)

Audit across all 8 topic directories. Summary table:

| Directory | Total issues | Broken-image-refs | Thin captures | Collisions |
|---|---:|---:|---:|---:|
| `collective-bargaining-group-purchasing/` | 0 | 0 | 0 | 0 |
| `consumer-data-pooling/` | 33 | 33 | 0 | 0 |
| `dynamic-pricing-landscape/` | 2 | 2 | 0 | 0 |
| `lever-implementations/` | 3 | 2 | 0 | **1** |
| `obfuscation-deep-dive/` | 45 | 45 | 0 | 0 |
| `platform-cooperatives/` | 9 | 9 | 0 | 0 |
| `price-transparency-tools/` | 2 | 2 | 0 | 0 |
| `seller-algorithm-taxonomy/` | 34 | 34 | 0 | 0 |
| **TOTAL** | **128** | **127** | **0** | **1** |

**Broken image refs** (127 of 128 issues): all originate from `capture_pdf --engine pymupdf` not extracting figures. Text content of each capture is intact, and every captured claim cited on the wiki is sourced from text, not figures. Not load-bearing. Consistent pattern across all arXiv captures where marker engine was unavailable (GPU-OOM fallback).

**Image-path collision** (1 issue): `raw/research/lever-implementations/assets/ac44065f938ab450.png` is referenced by both `09-09-wikipedia-noyb.md` and `12-04-wikipedia-privacy-badger.md`. Investigation: the image is Wikimedia's "Edit this at Wikidata" UI icon (476 bytes), a generic shared asset. Both Wikipedia pages legitimately embed the same icon. **Benign — not a cross-paper content overwrite.** No action needed; the audit tool's collision detector is working as designed but the result is a false-positive-for-concern here.

---

## Stray / Noise Files — **NEW CATEGORY**

Six 0-byte stray `.md` files at `wiki/` root that do NOT belong in the wiki:

```
wiki/2026-04-23.md
wiki/adnauseam.md
wiki/algorithmic-collusion.md
wiki/lever-implementation-readout.md
wiki/obfuscation-strategic-readout.md
wiki/possible-strategic-levers.md
```

All are 0 bytes. Canonical pages for each exist in the correct subdirectories (`tools/adnauseam.md`, `mechanisms/algorithmic-collusion.md`, `strategies/lever-implementation-readout.md`, `strategies/obfuscation-strategic-readout.md`, `strategies/possible-strategic-levers.md`). The `2026-04-23.md` has no canonical — presumably from clicking a date-format wiki-link somewhere.

Most likely cause: **Obsidian auto-creates a `.md` file at the vault root when a user clicks an unresolved `[[wiki-link]]`** and Obsidian doesn't know where the resolver expects the file. The vault has `.obsidian/` enabled (per git status at session start).

Because these are empty, they don't pollute the link graph, capture audits, or the render. They are pure filesystem noise. Recommend deletion:

```bash
rm wiki/2026-04-23.md wiki/adnauseam.md wiki/algorithmic-collusion.md \
   wiki/lever-implementation-readout.md wiki/obfuscation-strategic-readout.md \
   wiki/possible-strategic-levers.md
```

(Logged as a candidate kit-level finding for `master_notes.md` — the lint process could detect 0-byte noise files itself.)

---

## Summary Dashboard

| Category | Status | Count |
|---|---|---:|
| Orphan pages | ✅ Clean | 0 |
| Broken links | ✅ Clean | 0 (2 placeholders in CLAUDE.md only) |
| Format issues | ✅ Clean | 0 |
| Conflicts | ✅ Clean | 0 |
| Un-ingested raw sources | ✅ Clean | 0 |
| Coverage gaps | ⚠️ Flagged | 2 conceptual (surveillance-retail counter-power thin; data-broker layer) |
| Stale content | ⚠️ Flagged | 4 (markup-citizen-browser, algorithmwatch, drivers-seat-coop, open-food-network) |
| Source metadata (10a) | ✅ Clean | 0 |
| Conceptual cross-links (10b) | ✅ Clean | 0 |
| Industry ↔ counter-power (10c) | ⚠️ Flagged | 1 (surveillance-pricing-retail) |
| Claim attribution (10d) | ⚠️ Flagged | 2 (algorithmic-collusion 28% margin; park-slope 66.43% vote) |
| Stale tech (10e) | ⚠️ Flagged | 4 (same as stale-content) |
| Capture fidelity — broken image refs | ℹ️ Known | 127 (pymupdf limitation; text intact) |
| Capture fidelity — thin captures | ✅ Clean | 0 |
| Capture fidelity — collisions | ℹ️ Benign | 1 (shared Wikimedia UI icon) |
| Stray 0-byte files at wiki/ root | ⚠️ Flagged | 6 (Obsidian artifacts, safe to delete) |

Four flagged categories (Coverage gaps, Stale content, 10c, 10d), plus stray-files. All fixable in targeted small passes — none blocks normal wiki operation.
