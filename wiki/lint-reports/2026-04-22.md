# Lint Report — 2026-04-22

Wiki at `/home/david/code/wiki-agentic-trends/wiki/`. 27 markdown files across 11 directories (including 4 root special pages, 2 empty reserved dirs, and a new `lint-reports/` dir for this report).

## Summary

| Category | Status | Count |
|---|---|---|
| Orphan pages | ✅ clean | 0 |
| Broken wiki-links | ✅ clean | 0 |
| Broken section anchors | ⚠ | **2** (both point at bullets, not headings) |
| Format compliance | ✅ clean | 0 |
| Coverage gaps (un-ingested raw files) | ✅ clean | 0 |
| Stale content | ✅ clean | 0 |
| Date-stamping | ⚠ | 2 minor |
| Model / version pinning | ⚠ | 1 minor |
| Source-bias tagging | ✅ clean | 0 |
| Evidence-class labelling | ✅ clean | 0 |
| **Personnel linkage** | ⚠ | **significant debt — 3 ingests deferred** |
| Capture fidelity (raw/) | ✅ clean | 0 across all 3 topic dirs |

## Orphan Pages

None. Every content page has at least one inbound link from another page. Exempt pages (`index.md`, `CLAUDE.md`, `revisions.md`, `log.md`) are not checked.

## Broken Links

Plain `[[page]]` links: none broken.

### Broken section anchors (2)

Both point at named items that exist as *bullets* inside a parent section, not as their own `## heading`, so Obsidian's anchor resolver won't find them.

1. **`[[failure-modes#metric-masked-quality-degradation]]`** — referenced from 3 pages:
   - `trends/2026-04-snapshot.md:47`
   - `case-studies/klarna.md:52`
   - `practices/escalation-vs-approval.md:47`
   Target: bullet "**Metric-masked quality degradation.**" inside `## Evaluation traps`. Fix: promote to `### Metric-masked quality degradation` subheading.

2. **`[[failure-modes#atomicity-failure-in-multi-step-workflows]]`** — referenced from:
   - `deployments/production-deployments.md:34`
   Target: bullet "**Atomicity failure in multi-step workflows.**" inside `## Architecture-level failures`. Fix: promote to `### Atomicity failure in multi-step workflows` subheading.

**These are being fixed in this lint run.**

### Not-bugs (flagged by scan, confirmed correct)

Backslash-pipe escapes like `[[slm-agents\|SLMs]]` in table cells are **correct Obsidian syntax** — the pipe is a column delimiter in markdown tables, so wiki-link aliases need escaping. Locations (all valid):

- `patterns/fine-tuning-vs-context-engineering.md:7`
- `deployments/production-deployments.md:45,51,54`

## Format Compliance

All 23 content pages pass format compliance:

- `# Title` at top ✅
- Summary paragraph ✅
- `## Source` section ✅
- `## Related` section with at least one `[[wiki-link]]` ✅

Root specials exempt (`index.md`, `CLAUDE.md`, `revisions.md`, `log.md`).

## Coverage Gaps

**Zero un-ingested raw source files.** All 20 raw markdown files under `raw/research/` are cited by at least one wiki page. Nothing to auto-ingest.

### Conceptual gaps (already documented)

Explicit gap pages already exist for these:

- `gaps/ft-vs-context-engineering.md` — remaining sub-questions on absolute cost figures, named-practitioner takes, long-context-vs-FT head-to-head.
- `gaps/enterprise-data-integration.md` — schema discovery, governance, provenance, enterprise retrieval beyond vector DBs.
- `deployments/production-deployments.md` — needs independent third-party replications, enterprise engineering blogs, failure post-mortems, named-practitioner FT-vs-RAG.

No new conceptual gaps surfaced by this lint.

## Stale Content

None detected. No `TODO` / `FIXME` markers, all raw-source paths in `## Source` sections resolve, no future-tense references to already-completed work, versioned references correctly dated.

## Domain-Specific Issues

### Date-stamping (minor)

- `patterns/slm-agents.md:7` — "NVIDIA's working threshold: <10B parameters (2025)" — year precision. Source is arXiv 2506.02153 (June 2025); could tighten to "(2025-06)".
- `patterns/slm-agents.md` (pillars section) — benchmark comparisons like "Phi-2 (2.7B) ~15× faster than 30B on commonsense/code" and "DeepSeek-R1-Distill-7B matches or outperforms Claude-3.5-Sonnet on some tasks" — no date stamp on the comparative claim itself. The source paper is dated, but the specific benchmark vintage isn't.

### Model / version pinning (minor)

- `patterns/slm-agents.md` — "Claude-3.5-Sonnet" lacks a release tag. Claude 3.5 Sonnet had 2024-06 and 2024-10 releases. Context clear from source but tag would tighten.

### Source-bias tagging

Clean. Every lab / vendor source is named with its bias flag — NVIDIA, Anthropic, LangChain, PromptLayer, Fireworks, Google OCTO, and ByteByteGo (third-party) are each tagged where they appear.

### Evidence-class labelling

Clean. All substantive claims carry evidence class (`tested`, `practitioner-consensus`, `speculative`, or equivalent descriptors like "cited", "benchmarked", "survey synthesis"). No untagged claims spotted.

### Personnel linkage — significant debt

**No `wiki/people/` directory exists.** Third consecutive ingest deferring personnel stubs. Lint rule 14 is firing on almost every page that names a practitioner.

Individuals named across ≥2 wiki pages (stub candidates):

- **Hamel Husain** — 7 pages (`measurement-vs-architecture`, `error-analysis`, `llm-as-judge`, `failure-modes`, `benchmarks`, `ft-vs-context-engineering`, `enterprise-data-integration`).
- **Shreya Shankar** — 4 pages (`error-analysis`, `llm-as-judge`, `failure-modes`, `ft-vs-context-engineering`).
- **Simon Willison** — 3 pages (`agentic-engineering`, plus snapshot + references).
- **Erik Brynjolfsson / Elisa Pereira / Alvin Wang Graylin** — Stanford playbook authors, cited across `stanford-51-enterprise-playbook`, `measurement-vs-architecture`, `framework-skepticism`, `production-deployments`.
- **Peter Belcak / Greg Heinrich** — NVIDIA SLM paper leads, cited across `slm-agents`, `fine-tuning-vs-context-engineering`, `production-deployments`.
- **Qizheng Zhang** — ACE paper lead, cited on `agentic-context-engineering`, `fine-tuning-vs-context-engineering`.
- **Erik Schluntz / Barry Zhang** — Anthropic authors, cited on `building-effective-agents`.
- **Sebastian Siemiatkowski** (Klarna CEO) — cited on `klarna`, `escalation-vs-approval`, `failure-modes`.
- **Will Grannis** (Google Cloud OCTO) — cited on `production-deployments`.

**Status:** deferred by user direction across three research runs. Lint rule 14 flags this as accumulating debt; fix requires a dedicated pass (create `wiki/people/` + per-person stub pages + update linking pages to use the stubs).

## Capture Fidelity (raw/)

All three topic directories clean — zero broken image refs, no unpaired markdowns, no thin captures, no image-path collisions.

- `raw/research/effective-agentic-patterns/` — 0 issues
- `raw/research/fine-tuning-vs-context-slms/` — 0 issues
- `raw/research/production-slm-case-studies/` — 0 issues

## Content Observations

Strengths surfaced by the read-through:

1. **Graph connectivity is excellent** — no orphans, dense cross-linking, pattern → case-study → failure-mode dependencies well-wired.
2. **Evidence-source hygiene is strong** — every empirical claim traces back to an arXiv ID, publication date, or vendor-flagged source.
3. **Synthesis is explicitly marked** — statements that are Claude's cross-source reading are labelled *(synthesis)* or *(editorial)* rather than laundered into source voice.
4. **Tensions aren't smoothed** — `measurement-vs-architecture.md` explicitly documents the Hamel-vs-Anthropic framing without picking a winner.
5. **Production exemplars ground the abstractions** — the April 2026 case-study ingest anchored `slm-agents`, `escalation-vs-approval`, and the FT-vs-context decision framework to named deployments.
