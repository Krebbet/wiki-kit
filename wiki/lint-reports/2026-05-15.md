# Lint Report — 2026-05-15

First persisted lint run. Wiki: 173 markdown files, 3468 `[[wiki-links]]`, 0 orphans. Mechanical link/format analysis via script; capture fidelity via `tools.audit_captures`; semantic checks via 3 parallel theme-cluster subagents.

### Orphan Pages

None. Every non-exempt page has ≥1 inbound `[[link]]`.

### Broken Links

**Genuine (fix recommended) — 5:**

| Page | Broken target | Cause |
|---|---|---|
| `research/rlvr-mechanics/binary-rewards-rl-challenges.md` | `[[]]` | Empty link literal |
| `research/rlvr-mechanics/rethinking-rl-sparse-selection.md` | `[[]]` | Empty link literal |
| `research/selective-finetuning/_overview.md` | `[[../catastrophic-forgetting/_overview]]` | Target file does not exist (theme has only the `ewc-gemma2-cpt` seed page) — introduced 2026-05-13 |
| `research/synthesis/fine-tuning-best-practices.md` | `[[../rl-optimizers/bolt-kl-rlvr-boltzmann\]]` | Malformed — escaped closing bracket `\]]` |
| `research/teacher-student-rl/rlt-followups-2026.md` | `[[../teacher-student-rl/opsd-self-distilled-reasoner]]` | No such page; the OPSD content lives in `opsd-compresses-rlvr.md` (and refers to a *different* OPSD paper — see Conflicts B) |

**Convention, not broken (~75):** bare `[[_overview]]` from per-paper pages to their theme overview. Obsidian resolves these by same-folder proximity; the script flags them only because `_overview` is a non-unique basename. Wiki-wide convention, intentional, not fixed. (Noted as a soft style item — see Domain-Specific.)

**Exempt placeholders (not real):** `CLAUDE.md` (`[[page-name]]`, `[[wiki-link]]`), `log.md` / `revisions.md` historical example links, `reference-sources.md` (`[[../weekly-briefs/<YYYY-MM-DD>]]` template). These files are lint-exempt by spec.

### Missing Cross-References

- `research/synthesis/recursive-concept-learning.md` — components **T2** (per-prereq EWC) and **T3** (mask composition) discuss exactly the selective-finetuning territory but do not `[[link]]` `[[../selective-finetuning/_overview]]` or the 2026-05-13 locked composition stack (which lives only in `proposed-method.md`).
- `research/synthesis/single-sample-concept-skeleton.md` — "Design space" direction (d) discusses orthogonal-subspace allocation without linking `[[../selective-finetuning/o-lora]]`; the skeleton is a common entry point.
- `research/synthesis/concept-curriculum-method.md` line ~175 (textbook-size-vs-context-window) discusses the **C_w** territory without a forward link to the `proposed-method.md` C_w extension; the reverse link already exists (asymmetric).
- `research/teacher-student-rl/rlt-followups-2026.md` — no link to `[[opsd-compresses-rlvr]]` despite both covering "OPSD" (see Conflicts B).
- `research/concept-evaluation/_overview.md` — "Hewitt&Liang" invoked by bare author name on the under-specification clause without linking `[[control-tasks-probes]]` on that clause (the page is linked elsewhere in the paragraph).
- `research/single-sample-rl-finetuning/_overview.md` — 2026-05-13 forward links to `[[../decoding-time-steering/actadd]]` and `[[../selective-finetuning/skill-localization]]` are one-directional; no reciprocal backlink from those pages to the single-sample theme.

### Format Issues

**Research-page schema gaps (missing `## Method` and/or `## Claims`):** `rl-optimizers/latent-grpo.md`, `rl-optimizers/tsallis-loss-continuum.md`, `rlvr-mechanics/binary-rewards-rl-challenges.md`, `self-play/sgs.md`, `teacher-student-rl/mad-opd.md`, `teacher-student-rl/opsd-compresses-rlvr.md`, `teacher-student-rl/co-evolving-policy-distillation.md`, `teacher-student-rl/rlt-followups-2026.md`, `test-time-training/tempo.md`, `selective-finetuning/knowledge-editing-survey.md`, `rl-optimizers/bolt-kl-rlvr-boltzmann.md`, `rlvr-mechanics/rethinking-rl-sparse-selection.md`.

**Pattern:** every one of these except `knowledge-editing-survey` was a *weekly-brief* capture or a survey/landscape page. Weekly-brief uses a lighter page schema than `/ingest` (uses `## Results` / `## Empirical results` / `## Taxonomy` instead of `## Claims`; folds method into the summary). This is a **kit-level pattern**, not 12 independent defects — flagged to `master_notes.md` (see Harvest).

**Missing `## Related`:** `research/synthesis/decoding-time-shapes.md` has a `## Connections` section instead. Minor; add a `## Related`.

**Missing `## Source`:** `conflicts/invisible-leash-vs-spiral-transfer.md`, `conflicts/unified-vs-two-model-self-play.md` — conflict files use a different schema by design; not a true defect, but a `## Source` listing the contested pages would help.

`weekly-briefs/*.md` lack `## Source`/`## Related` — these are brief *outputs*, not research pages; expected.

### Conflicts

**A. `rlvr-incentivizes-reasoning` misattributed and inverted across ≥4 pages (HIGH).** The page itself is correct: **Wen et al. 2025, arXiv:2506.14245**, which *pushes back on* Yue — it argues RLVR genuinely *extends* the reasoning boundary (CoT-Pass@K on AIME/coding), i.e. it is **evidence against Position A** (the pure-reweighting thesis). But:
- `index.md` line 40 attributes it to "**Shao et al.**" and summarises it as "RLVR *selects for* correct-reasoning CoT priors already present" — the **opposite** of the paper's claim.
- `self-play/invisible-leash.md` (Related), `self-play/yue-rlvr-boundary.md` (Related), `self-play/_overview.md` — cite it as "Shao et al." *supporting* Position A.
- `conflicts/invisible-leash-vs-spiral-transfer.md` describes it as "sympathetic to A" — backwards.
This is a paper being cited as support for the thesis it refutes, under the wrong author. **Verified against the page's own `## Source` (arXiv:2506.14245, Wen et al.).** Recommend fixing index summary + the 3 self-play references + the conflict-file framing.

**B. "OPSD" name collision, undisambiguated (MEDIUM).** Two different papers carry the "OPSD" label and are cross-referenced as if one method:
- `rlt-followups-2026.md` §4 — Zhao et al., *Self-Distilled Reasoner*, arXiv:2601.18734 (Jan 2026); claim: 4–8× token efficiency over GRPO.
- `opsd-compresses-rlvr.md` — arXiv:2605.06188 (May 2026); claim: compaction, −1.0 pp on Correct-only, *not* an efficiency method.
Neither page links the other or flags the collision. Recommend a disambiguation note on both pages + a name qualifier (e.g. "OPSD-SDR" vs "OPSD-Compresses").

**C. `process-reward-models/_overview.md` conflates ORM-RL and PRM-RL (MEDIUM).** The Uesato-2022 table row reports 12.7 / 3.4 (final/trace error) under a combined "ORM-RL, PRM-RL" cell. Per the source page `process-outcome-feedback.md`, 12.7/3.4 is the **ORM-RL** result; PRM-RL is 12.9/3.8 (worse on both). The overview's combined label assigns ORM numbers to both and obscures that ORM-RL beats PRM-RL on this benchmark.

**D. `proposer-reward-shapes.md` re-litigates a locked decision (MEDIUM, also Stale).** Line ~55 marks component C (reference-in-context during gradient steps) as "**Open. Decision pending.**" — contradicted by `proposed-method.md` gap §4 "**Decision locked 2026-05-01:** structural over soft" (recorded in memory `feedback_self_play_design_choices.md`). Should read "Locked 2026-05-01: structural asymmetry."

No conflicts found that contradict the two open conflict files' recorded positions (other than the Position-A miscitation in A above, which sharpens `invisible-leash-vs-spiral-transfer`).

### Coverage Gaps

**Conceptual (human judgement — surfaced, no auto-action):**

- **`catastrophic-forgetting/` is a single seed page, treated as a full theme.** Referenced as a load-bearing theme from ≥12 pages + the index themes table (on equal footing with `selective-finetuning/`, `rlvr-mechanics/`), and `selective-finetuning/_overview.md` links a non-existent `[[../catastrophic-forgetting/_overview]]`. Either (a) write a `catastrophic-forgetting/_overview.md` (EWC + the selective-finetuning gradient-masking siblings + O-LoRA as the structural alternatives — much of the material already exists), or (b) downgrade the index entry to "seed". Recommend (a).
- **`self-improvement/_overview.md`** synthesis table covers STaR / Self-Rewarding LM / rStar-Math but omits **PAG** (`multi-turn-policy-verifier`), a listed page. Four-paper theme, three-row table.
- **`critique-self-correction/_overview.md`** synthesis tables omit **Prometheus-2** and **Critic-CoT** (2 of 6 listed pages). Same pages-listed-but-not-integrated pattern.
- **α-DPG / forward-KL diversity-collapse fix.** `binary-rewards-rl-challenges.md` names α-DPG (Kruszewski et al. ICLR 2026) and Li et al. arXiv:2509.07430 as the only principled structural fix to binary-reward mode-collapse — central to the wiki's single-sample concern — but they appear only as bare citations with no page and no `/research` capture. Candidate next research target.
- **Thinking-pattern-consistency / top-k-overlap OPD success condition.** Cited as a load-bearing design constraint across `rlt-followups-2026`, `co-evolving-policy-distillation`, `opsd-compresses-rlvr`, but absent as an axis in `teacher-student-rl/_overview.md`'s method-comparison table and has no synthesis note.

**Un-ingested raw sources:** none. All 15 `raw/research/` topic dirs are wiki-referenced. `raw/research/dataset-references/learning-from-less` is intentionally *noted-not-ingested* per the user's 2026-05-14 ruling and is referenced from `concept-evaluation/_overview.md` — no auto-ingest action taken (user ruling overrides the lint auto-ingest default).

### Stale Content

- `proposer-reward-shapes.md` line ~55 "Open. Decision pending" — overtaken by the 2026-05-01 lock (see Conflicts D).
- `rlvr-mechanics/_overview.md` Open Questions: the L2T "does info-gain reward help in single-sample regime" and "accuracy peaks at ~16–20 episodes" questions are *partially* resolved elsewhere (`tsallis-loss-continuum.md` cold-start escape at $q{=}0.75$; `two-stage-dynamic.md` Stage-1 entropy-collapse mechanism) but not back-annotated. Soft — partial resolutions, worth a "see also" rather than deletion.
- No expired TODOs or superseded-design references found beyond the above. `recursive-concept-learning.md` T2/T3 "Untested" labels are accurate, not stale.

### Domain-Specific Issues

- **Paper identifiers:** all research per-paper pages carry an arXiv ID / venue in `## Source` (the script's "no paper identifier" flags on `bolt-kl-rlvr-boltzmann`, `rethinking-rl-sparse-selection`, `binary-rewards-rl-challenges`, `mad-opd`, `opsd-compresses-rlvr` are false positives — the ID is in the summary/`## Source` as a raw-path or first-line arXiv, just not matching the regex window). No action.
- **Conceptual cross-links:** healthy — 3468 links across 173 pages; the recently-added themes (decoding-time-steering, selective-finetuning) are densely cross-linked into the existing graph.
- **Quantitative claims cite figures:** spot-checks clean (ROME Tables 1/4, ITI Table 2, MEMIT, REASONMAXXER cost table all cite specific tables/figures).
- **Recency:** no fast-moving page is sourced only on >5yr-old work without recent follow-up. PackNet (2018) / HAT (2018) / options-framework (1999) / Bengio-curriculum (2009) are correctly framed as foundational alongside 2023–2026 work.
- **Bare `[[_overview]]` convention:** ~75 instances. Functional in Obsidian (proximity resolution) but technically ambiguous given 16+ `_overview.md` files. Wiki-wide convention; flag as a known soft item, not a mass fix. Candidate for a kit-level convention decision (use `[[../theme/_overview]]` explicit form on new pages).

### Capture Fidelity (raw/)

`tools.audit_captures` run on all 15 topic dirs. **377 total flagged issues, 100% of category "broken image refs", 0 thin captures, 0 missing PDFs, 0 image collisions.**

| Topic dir | Broken-ref count | Other |
|---|---|---|
| self-play-concept-learning | 89 | clean |
| decoding-time-steering | 86 | clean |
| concept-understanding-eval | 47 | clean |
| rcl-gap-fillers | 45 | clean |
| selective-finetuning | 28 | clean |
| self-play-quality-extraction | 25 | clean |
| rl-optimizers | 20 | clean |
| weekly-2026-05-03 | 20 | clean |
| weekly-2026-04-23 | 11 | clean |
| rlt-followups | 5 | clean |
| weekly-2026-05-10 | 1 | clean |
| adjacent-reward-signals, dataset-references, single-sample-llm-learning, teacher-student-reasoning-rl | 0 | clean |

**All 377 are the documented pymupdf wrong-anchor bug** (master_notes 2026-05-13): image files exist on disk, but MD-embedded paths are anchored at repo-root rather than file-relative, so `audit_captures` (which resolves `(md.parent / ref)`) sees a doubled path. Spot-verified: `self-play-concept-learning/assets/01-alphazero/source.pdf-3-0.png` is present on disk. **Text content of every capture is intact** (0 thin, 0 missing-PDF). Rendering-only; no re-capture needed; no downstream wiki page is affected (wiki pages don't embed these images). The scale (377) makes /lint perpetually noisy until the kit fix lands — reinforces priority of master_notes candidate-fix #2 (audit special-cases the wrong-anchor pattern so the audit-clean signal stays honest).

### Trend Radar

Active threads, by momentum:

1. **R_w hypothesis consolidation (dominant).** "Information is already in the base model; an offline reweighting prior suffices" — now triangulated across three independent corpus directions: decoding-time-steering (13 papers, 2026-05-13), selective-finetuning (15 papers, 2026-05-13), RL-emergent sparsity (Balashov / REASONMAXXER). Localisation-scale convergence sharpening: 0.01% (Skill-Localization) / 0.04% (REASONMAXXER $W_O$ rank-8) / 5–30% (Balashov). This is the spine the proposed-method R_w extension now stands on.
2. **Selective-gradient composition stack.** PIT → Skill-Loc mask → O-LoRA subspace → ROME/MEMIT edit → AlphaEdit null-space projection, locked as a complementary stack (design ruling 2026-05-13). Untested end-to-end — the live experimental question.
3. **Entropy-gate vs divergence-gate equivalence (REASONMAXXER × OPD).** Flagged 2026-05-13: both index the same active-position set through different signals. Crossover documented; composition untested.
4. **Variable-granularity concept architectures.** Hypothesis seeded 2026-05-13 (`synthesis/concept-granularity-architecture.md`); watchlist section has 7 categorical targets, **zero captures** — pure radar, no corpus yet.
5. **Diversity-collapse structural fix (α-DPG / forward-KL).** Surfaced in `binary-rewards-rl-challenges` as the only principled fix; uncaptured. Rising relevance to the single-sample concern.

Watchlist state: *Decoding-time / activation steering* section cleared (9 → 1, promoted to theme). *Variable-granularity* newly seeded (7, unfilled). *Post-GRPO RL optimizers* (5 entries) and *Teacher-student RL* (5 entries) are the densest unpromoted sections — candidates for promotion if ≥2 more converge.

### Radar Evolution

Baseline entry — first persisted lint report; no prior radar to diff against. In-session evolution observed: the *Decoding-time / activation steering* watchlist section completed the full lifecycle (seeded 2026-05-12 with 9 entries → promoted to a 13-paper theme 2026-05-13 → cleared to 1 residual). The *selective-finetuning* theme was created without a prior watchlist incubation (direct /research from a user question). The *R_w hypothesis* moved from a single /query note (2026-05-12) to a triangulated three-theme backbone (2026-05-13) in three days — the fastest-consolidating thread in the wiki's history to date. Future lint runs should diff Trend Radar against this section.

---

*Generated by /lint 2026-05-15. Mechanical analysis: scripted link-graph + format. Capture fidelity: tools.audit_captures × 15 dirs. Semantic checks: 3 parallel theme-cluster subagents. Highest-stakes finding (rlvr-incentivizes-reasoning miscitation) verified against the page's own `## Source` before reporting.*

---

## Resolution log (applied 2026-05-16)

- **HIGH rlvr-incentivizes-reasoning miscitation — FIXED.** Corrected author ("Shao et al." → "Wen et al.", arXiv:2506.14245) and inverted positioning (it argues *against* Position A via CoT-Pass@K, not for it) across: index.md, self-play/invisible-leash.md, self-play/yue-rlvr-boundary.md, self-play/_overview.md (×2), conflicts/invisible-leash-vs-spiral-transfer.md.
- **Broken links — 1 fixed, 4 reclassified as lint-script false positives.** Genuine: rlt-followups-2026 `[[opsd-self-distilled-reasoner]]` (aspirational page) → delinked + OPSD name-collision note added. False positives (no wiki change; kit issue filed): `[[#Conflicts]]` in-page anchors ×2, the documented Obsidian table-escaped-pipe `[[x\|y]]` ×1, and the catastrophic-forgetting/_overview link (auto-resolved — the 2026-05-16 research run created the file).
- **OPSD name collision — FIXED.** Reciprocal disambiguation notes on rlt-followups-2026.md (Zhao 2601.18734) and opsd-compresses-rlvr.md (2605.06188).
- **proposer-reward-shapes component-C stale lock — FIXED.** "Open. Decision pending" → "Locked 2026-05-01: structural asymmetry".
- **catastrophic-forgetting single-seed gap — RESOLVED** by the 2026-05-16 research run (1 seed → 8-page theme + _overview).
- **PRM ORM/PRM-RL conflation — FIXED.** Verified vs process-outcome-feedback.md:21; table cell now distinguishes ORM-RL 12.7/3.4 from PRM-RL 12.9/3.8.
- **Missing cross-refs — FIXED.** RCL T2/T3 → selective-finetuning + catastrophic-forgetting + loramoe; single-sample-concept-skeleton dir-(d) → o-lora/moe-adapters; concept-curriculum-method → proposed-method C_w⁺; concept-evaluation Hewitt&Liang → control-tasks-probes.
- **decoding-time-shapes.md ## Related — FIXED** (renamed Connections → Related).
- **Conflict files ## Source — ADDED** to both open conflict files (contested-page listings).
- **Weekly-brief research-page schema gaps (12 pages) — NOT per-page-fixed by design:** dispositioned to master_notes 2026-05-15 as a kit-level `/weekly-brief`↔`/ingest` template mismatch (mass-adding empty `## Method`/`## Claims` headings would fight the originating template and add no content value). Awaiting `/harvest`.
