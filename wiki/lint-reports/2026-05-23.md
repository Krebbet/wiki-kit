# Lint Report — 2026-05-23

Wiki: 36 content pages. Deterministic checks (orphans/links/format) scripted; content-judgment checks via analysis pass.

### Orphan Pages
- None. Every content page has ≥1 inbound `[[link]]`.

### Broken Links
- None real. 3 false positives: `watchlist.md → [[conflicts/lidar-vs-vision-autonomy]]` is a valid Obsidian path-qualified link (resolves; cosmetic inconsistency vs the bare `[[lidar-vs-vision-autonomy]]` used elsewhere); the two `[[page-name]]`/`[[wiki-link]]` in `CLAUDE.md` are format-doc examples.

### Missing Cross-References
- `aerial-perching` — mentions "event camera" (flapping-wing perching) without linking `[[event-cameras]]`; grasping-mechanism section doesn't link `[[aerial-grasping]]` on first mention (only in Related).
- `cooperative-aerial-manipulation` — references gecko/microspine + grasping/perching mechanics but links neither `[[aerial-grasping]]` nor `[[aerial-perching]]`.
- `air-water-robots` — GNSS-denied underwater positioning not linked to `[[visual-inertial-slam]]`.
- `neuromorphic-materials` — relies on DVS/event input throughout but never links `[[event-cameras]]`.
- `safe-indoor-flight` — cites FAA Operation-Over-People but doesn't link `[[faa-part-108-bvlos]]` / `[[detect-and-avoid]]`.

### Format Issues
- None. All pages have title + summary + `## Source` + `## Related`.

### Conflicts
- **Staleness inconsistency (not source-vs-source):** `faa-part-108-bvlos` still reads as "proposed rule, ~Feb 2026 deadline upcoming"; the watchlist (Run 2026-05-17) records the deadline slipped with no final rule. The page lags the watchlist — needs a status update.
- `conflicts/lidar-vs-vision-autonomy` — no unflagged contradiction; Dream-to-Fly (2026-05-17) correctly filed as Position-B with the real-camera caveat.

### Coverage Gaps (conceptual — no un-ingested raw; `raw/` is empty/cleaned)
- **Use-case application layer entirely absent** (biggest structural gap): no pages for inspection / delivery / agriculture / mapping / SAR despite being in scope and in the 6-month goal. AI-capability pages have no use-case pages to link (drives the intersection-coverage asymmetry below).
- **EASA / Transport Canada BVLOS** referenced as a contrast (Canada "~12–18 mo behind") but no page; given the Canadian scope a Transport Canada BVLOS page would earn its keep.
- **Battery / energy-density roadmap** — cited as the binding endurance ceiling (`drone-power-budget`) and behind Volatus's claimed domestic battery supply, but no dedicated page.

### Stale Content
- `skydio-autonomy-stack` — 2020 vendor source; the page flags TX2 as aged but not that Skydio's standing as a *consumer* existence-proof may have drifted (needs a re-verification caveat; specific status claims need a source, not assertion).
- `faa-part-108-bvlos` — ~Feb 2026 final-rule deadline has now passed; page implies rule still pending. Update needed.
- `drone-power-budget` — open TODO ("no modern DNN-inference power decomposition for a mid-size consumer drone") persists; route to watchlist if not already.
- `aerial-manipulation` / HKU continuum-manipulator — deferred-source ("needs manual PDF download") persistently unresolved; on watchlist.

### Domain-Specific Issues
- **(10) Regulatory staleness:** `faa-part-108-bvlos` (NPRM Jun 2025; deadline passed; no re-verify note) — **flag**; `detect-and-avoid` (ASTM F3442 2023, ACAS sXu MOPS live process; no re-verify note) — **flag**; `canadian-drone-onshoring` (TC Drone Zone Dec 2025) — add re-verify-by note.
- **(11) Benchmark staleness:** `skydio-autonomy-stack` (~2020, no body freshness note) — flag; `cooperative-aerial-manipulation` (arXiv 2403.14347, >13 mo, no "as of 2024" note) — flag; `lidar-for-uav-autonomy` (Sept 2025 survey, ~8 mo — re-check ~Jun 2026). 6 newest pages clean.
- **(12) Company-status drift:** `skydio-autonomy-stack` — material change likely (consumer-line/enterprise pivot) but unsourced; add caveat + research flag. `drone-manufacturing-supply-chain`/Draganfly — "operational (2023 PR)" with no re-check. Anduril, Volatus (caveated) clean.
- **(13) Intersection coverage:** **no use-case pages exist** → all AI-capability pages are one-sided. Single biggest structural gap vs the wiki's goal.
- **(14) Conflict freshness:** `lidar-vs-vision-autonomy` last updated 2026-05-17 (6 days) — clean.
- **(15) Evidence-strength tags:** 6 newest pages + skydio/anduril clean. **`cooperative-aerial-manipulation` has NO evidence tags** — flag (add *demoed/claimed* throughout).
- **(16) Manufacturing/origin tags:** `skydio-autonomy-stack` (no country-of-origin / NDAA note), `anduril-lattice` (no origin/Blue-UAS note), `drone-manufacturing-supply-chain` (no Blue UAS/NDAA-848 column for Canadian OEMs serving US DoD) — flag.
- **(17) Canadian-onshoring freshness:** `canadian-drone-onshoring` + `drone-manufacturing-supply-chain` lack a `last-verified` header (ingested 2026-05-20). Add one.

### Capture Fidelity (raw/)
- N/A — `raw/research/` is empty (cleaned post-ingest). No topic dirs to audit; no un-ingested sources.

### Trend Radar
- **VLA-for-aerial-manipulation crystallising, entirely pre-commercial** — AIR-VLA / DroneVLA / LiteVLA-H / UAV-Flow within months; sim-to-real + onboard-inference are the gating steps.
- **Onboard open-vocabulary perception = the acknowledged binding blocker** with active research (aerial OVD ~28% F1) — highest-signal frontier for consumer aerial manipulation.
- **Non-cooperative DAA MOPS gap = the regulatory bottleneck**, still open; Part 108 final rule slipped past Feb 2026.
- **Consumer use-case application layer = the wiki's biggest structural absence** — the gap between research synthesis (done well) and the investable thesis the wiki is meant to support.
- **Notably absent:** battery/energy-density roadmap + solid-state-LiDAR cost trajectory — the two hardware variables most likely to move the consumer-autonomy timeline.

### Radar Evolution
- First lint with a Trend Radar; no prior baseline. Next lint: compare whether the use-case layer, VLA sim-to-real, and the DAA MOPS gap have moved.
