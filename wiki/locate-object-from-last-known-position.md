# Locating an Object from its Last Known Position — object records built for re-finding, the search escalation, and what verification is possible on a geometry-only sensor

**Summary.** A `locate_object(name)` that starts from a last-known location needs three things the object-side wiki does not yet supply: a record that stores **where the robot was standing when it last saw the thing** (not just where the thing was), a **search escalation** for when it is not there, and a **verification test** the rig's sensor can actually run. The classical object schemas — SLAM++/Fusion++/Quadric/Cube landmarks, Hydra/ConceptGraphs scene-graph nodes, ROS `vision_msgs/Detection3D` — store pose + extent + class + a confidence and **none of them stores the observing viewpoint or a usable last-seen time**; the systems that *do* store it are the 2025–26 memory agents built specifically to go back and look (STAR, EmbodiedLGR, "Where Did I Leave My Glasses?"), which is the precedent for adding that field here rather than an invention of this page.
The single most load-bearing empirical result is SLiM's (ICRA 2020): a **known but noisy static prior on object location made search *worse* than having no prior at all** (cup found 33 % of trials vs 83–100 %), and modelling that prior's uncertainty recovered the gap. A last-known location is exactly such a prior, so `locate_object` must treat it as a *hint with a decaying covariance*, never as a place the object is.
On a single 1.1 m LiDAR plane "identified" can honestly mean only **re-observed at the expected place with a compatible 2-D footprint** — a persistent handle, not a name; every published single-plane recogniser is a person/mobility-aid detector mounted at **30–40 cm**, and two of those papers state that **80 cm is already too high** — nothing in the literature operates at 1.1 m. Tilt sweeps buy coverage and geometry, not semantics; the camera is what buys identity, and even it is near a coin flip on two similar instances.

## Source

Web-verified 2026-09-22 (see *Sources*). ROS message fields were read from the `ros-perception/vision_msgs` repository and SLiM's results table from the paper PDF; the Spark-DSG / Khronos / Clio node schemas and thresholds quoted here were read from the MIT-SPARK source on `main`, not from the papers' prose. Project evidence: `docs/locked-map-office.md` (the locked map, relocalisation 0.5 cm / 0.4° median with a prior, cold 75 %), `eda/EDA226-N4-object-layers/FINDING.md` (permanent / semi-permanent / unverified by cross-map agreement with per-map visibility), `docs/next-campaign-scoping.md` (the human's five threads and the four open questions), `src/objmem/fingerprint.py` (the June record: `position`, `pin_err_m`, `provenance.last_seen_ts`, `n_observations` — no viewpoint, no extent, no state), `docs/sketches/rover-mapping-navigation-stack.md` (the guard shares the 1.1 m plane; drivable band ~1.9 × 1.2 m).

## Related

[[world-model-architecture]] (the layered form factor + the revisit loop this page's protocol instantiates) · [[object-fingerprint-memory]] (the per-instance re-ID bank and its thresholds) · [[semantic-object-memory]] · [[scene-graph-world-model]] · [[dynamic-object-handling]] · [[point-cloud-object-segmentation-models]] · [[3d-shape-completion-for-object-footprints]] · [[room-segmentation-floor-plan]] · [[stop-and-scan-room-mapping]] · [[stop-and-go-navigation-on-a-locked-map]] · [[tidybot-deep-dive]] · [[home-tidying-robots]] · [[active-slam-exploration]] (the frontier/next-best-view machinery the widening search reuses) · [[robust-evidence-mapping-principle]]

---

## 0. What the existing pages already settle — do not re-derive these

The June object campaign settled most of the *representation* question, and this page builds on it rather than repeating it:

- [[world-model-architecture]] settles the **layered form factor** (slow structural anchor / fast object layer / change-detection process / ephemeral live layer), the **Layer-2 record** (`instance_id`, metric `map_pose` + covariance, multi-view embeddings, metric dims, category, `last_seen_ts`, `state ∈ {present, moved, removed, uncertain}`, `version`, append-only `history`), the **revisit loop** (relocalise → re-detect → data-associate → classify add/moved/removed → version), the rule that **confidence decays rather than the record being deleted**, the **N-consecutive-miss guard** before committing `removed`, and the point that `τ_move` must sit above the pose-error floor. It also rejects dense volumetric/per-point-semantic form factors for this project. Its one weakness for the present purpose: it predates the rover and assumes hloc + stereo depth throughout.
- [[object-fingerprint-memory]] settles the **identity matcher**: DINOv2 ViT-S/14 over CLIP for instance-level re-ID, multi-view storage with max-over-views cosine, a precision-favouring threshold (provisionally 0.75), best-vs-second-best margin as a confidence signal, and the honest status that this is validated on synthetic colour-distinct objects only.
- [[semantic-object-memory]] and [[scene-graph-world-model]] settle the **scene-graph design space** (Hydra, ConceptGraphs, HOV-SG, Tag Map, DovSG — including DovSG's 94–95 % scene-change-detection accuracy); [[dynamic-object-handling]] settles the **three timescales** and their mechanisms (STVL, DynaSLAM, POCD's 80.2 % precision / 3.0 % FPR, iRobot's reject-and-revert down to 1.41 % failure).
- [[point-cloud-object-segmentation-models]] settles how to get **object proposals** out of our data (BEV connected components, not a learned 3-D model); [[3d-shape-completion-for-object-footprints]] settles **footprint completion** (retrieval + deformation); [[room-segmentation-floor-plan]] settles rooms and doors; [[tidybot-deep-dive]] and [[home-tidying-robots]] settle what the tidying systems do.

**What none of them answers** — and what this page is for: how an object record must be shaped so a robot can *go back and look*, what to do when it is not there, and what verification means when the rover's only sensor is a 2-D LiDAR plane.

## 1. What shipping and research systems actually store per object

| System | Pose | Extent | Identity | Appearance | Time | Confidence | Mobility prior |
|---|---|---|---|---|---|---|---|
| **SLAM++** (CVPR 2013) | 6-DoF in pose graph | from a **pre-scanned CAD model** | class = which database model | none (geometry matching) | none | detection score | none |
| **Fusion++** (3DV 2018) | 6-DoF; the pose graph is the *only* persistent map | per-object TSDF at object-dependent resolution + 3-D foreground mask | per-object instance | none | none | **existence probability** (for spurious detections) | none |
| **QuadricSLAM / CubeSLAM** (RA-L / T-RO 2019) | 9-DoF dual quadric, or 9-DoF cuboid | ellipsoid / oriented box | class from the detector; QuadricSLAM *assumes association solved* | none | none | measurement covariance / hand-tuned weight | CubeSLAM only — a motion model for dynamic objects |
| **Hydra / Clio** (IJRR 2024 / RA-L 2024) | node centroid in the graph | bounding primitive | open-set (Clio) | CLIP-class embedding | graph is rebuilt/optimised, not time-indexed per node | — | Clio's granularity is **task**-determined (Information Bottleneck) |
| **ConceptGraphs** (ICRA 2024) | centroid + point cloud | point cloud + box | instance node | **CLIP feature + caption** | detection count | detection count | none |
| **POCD** (RSS 2022) | object pose | TSDF model | instance | semantic class | updated per traversal | **stationarity score (Beta) + change magnitude (Gaussian)** | **yes — that is the stationarity score** |
| **Khronos** (RSS 2024) | object pose | mesh/TSDF | instance | semantic | **explicitly 4-D: fast short-term window + slow long-term factor graph** | — | implicit in the two-process split |
| **ROS `vision_msgs/Detection3D`** | `PoseWithCovariance` | `BoundingBox3D` (centre + size, 6-DoF) | `string id` "for consistency across messages", *may be empty* | **none** | header stamp only | per-hypothesis `score` | none |

Three things fall out — the first two established, the third this page's judgement.

**(a) The classical schemas store where the *object* was, never where the *robot* was.** None of SLAM++, QuadricSLAM, CubeSLAM, Hydra or ConceptGraphs records the pose the observation was made from. `vision_msgs` is the cleanest case: `ObjectHypothesisWithPose.pose` is the *object's* and is explicitly unstamped, the only time is the source frame's `header.stamp`, and the ROS ecosystem has **no standard persistent-object-memory message at all** — `moveit_msgs/CollisionObject` comes closest (stable `id`, subframes, incremental `ADD/MOVE`) but is a collision world, not a perception memory. Fusion++ is the near-miss: it keeps **BRISK snapshots binned at 15° of view-angle difference**, i.e. the viewpoint, but for relocalisation rather than for revisiting.

**(b) The systems built for re-finding *do* store it — and that is the precedent to copy.** STAR's long-term memory record is literally `(t, xₜ, embed(oₜ), oₜ)` — timestamp, **robot pose at observation time**, embedding, and the raw image, with a spatial index over the robot poses so the agent can ask what was visible from a place; it keeps the raw observation precisely so it can be revisited. EmbodiedLGR stores per node an embedded label plus **last-associated pose and last-observed time**. So `last_seen_from: {pose, tilt, range, incidence}` is not an invention — it is the field the 2025–26 memory-agent line converged on, and it converts "go to the object's coordinates" into "reproduce the observation that worked".

**(c) The permanence prior is what separates a map from a memory.** Among the classical schemas only POCD (stationarity score) and CubeSLAM (dynamic-object motion model) carry anything like it; the dedicated line makes it central — Rosen/Mason/Leonard's **persistence filter** (ICRA 2016) is the canonical form, storing last-observation time plus three sufficient statistics for exact constant-time Bayesian updates under a survival-time prior, and Perpetua (IROS 2025) extends it with *emergence* filters for things that come back. EDA226 already computes the rig's version — permanent / semi-permanent / unverified by cross-map agreement with per-map visibility — so the rig is, unusually, ahead of most schemas on exactly the field that says how much to trust a stale position.

Worth noting for calibration: **no shipping consumer system maintains object identity across sessions autonomously.** iRobot's persisted artefact is a user-confirmed **rectangle** (Image Review promotes an obstacle to a Keep-Out Zone — no class, no pose, no instance); Meta Quest persists per-anchor UUIDs across sessions but offloads the labelling to a human in Space Setup; Amazon Astro's durable object memory is user-taught doors and windows with an open/closed state. Where cross-session identity exists commercially, it is achieved by **relocalising the frame**, not by re-identifying the object — which is an argument for this rig's locked-substrate approach, since a correct frame reduces association to a within-frame nearest-neighbour test.

## 2. Where the object layer lives relative to a frozen map

Established practice is unambiguous: **a separate layer keyed to the metric map, never inside it.**

- Fusion++ makes the object pose graph the only persistent representation and keeps per-object volumes detachable, so one object's geometry can be re-integrated without touching the others; Panoptic Multi-TSDFs (ICRA 2022) generalises this to **submaps** at object granularity, so a changed object is a changed submap and the rest of the reconstruction is untouched.
- Khronos factorises the problem in *time*: a fast process for short-term dynamics inside an active window, a slow factor graph for long-term change — the frozen map is the slow process's output. iRobot's production lesson ([[dynamic-object-handling]]) is the same claim from the shipping side: the semantic layer is kept separate from and derived over the geometric map, with reject-and-revert when an update is inconsistent.
- Nav2 is the same pattern in the navigation stack: a static map plus *filters and layers keyed to it*, never a rewritten static map ([[stop-and-go-navigation-on-a-locked-map]]). Worth knowing before anyone proposes putting objects in the costmap: **every Nav2 layer writes a single `unsigned char` cost per cell and carries no identity whatsoever**, the costmap filters are raster PGM/PNG masks, and the spatio-temporal voxel layer stores exactly one double per voxel — the marking wall-clock time. Nav2's only persistent identified geometry is the Vector Object Server (`CircleObject`/`PolygonObject`: stable UUID + geometry + occupancy value, no class, no confidence, no time, hand-authored). The object layer has to be ours; there is nothing to inherit.

For this rig the ruling writes itself, and it also answers the scoping doc's open question 1: **objects live beside the locked map, keyed to the map frame, and nothing in the object layer is ever allowed to write back into `m1.json` or the station poses.** The map's job is to be the coordinate system in which object positions are expressed; if the object layer could move the map, every object position would be invalidated by every object update. The one legitimate feedback path is the EDA226 one — cross-map agreement *demoting* an asserted wall's semantics (W6) while leaving its geometry in the navigation grid.

## 3. Re-finding: the search problem, and the result that should scare us

The literature splits into two camps, with an awkward gap in the middle where `locate_object` actually lives:

- **Object-goal navigation** (Habitat ObjectNav, GOAT-Bench, InstanceImageNav) assumes an *unknown* environment and no prior map. Success rates are low — a 2024 method reports **39.0 % SR**, itself a 12.4-point improvement on the prior state of the art. These numbers are not this rig's problem: the rig has a map and a last-known pose, which is a strictly easier task.
- **Instance re-localisation between scans** (3RScan / RIO, ICCV 2019: 1482 RGB-D scans of 478 environments, task = recover the 6-DoF pose of a known object instance in a later scan) is the *right* task definition but is posed offline over two complete scans, not as something a robot does by driving. Its numbers are a caution: the best method recovers the instance's pose to < 0.2 m / 20° in **23.8 %** of cases (the widely-quoted 30.58 % is the *class-averaged* accuracy, a looser metric), with full RGB-D on both sides. Re-finding a moved object is not an easy problem that only the rover's sensor makes hard.

Between them sits classical **active object search**, whose taxonomy is the useful part: *direct search* (look where the target is believed to be — Garvey 1976) versus *indirect search* (first find a landmark the target is spatially related to, then search near it). Aydemir et al. (T-RO 2013) and Zeng et al.'s SLiM (ICRA 2020) are the reference systems; Zheng et al. (IROS 2021) give the POMDP formulation with an octree belief and a frustum field of view, demonstrated over two 10 m² × 2 m regions with objects at varying heights.

**SLiM's benchmark is the result to build the protocol around.** Searching for a cup with a *known but noisy static* prior on object locations succeeded in **33 %** of trials; the same search with **no** prior succeeded in **83 %**, and with the prior's uncertainty modelled ("known, dynamic") in **83 %**, and with the hybrid strategy in **100 %**. The static-prior run was not merely unhelpful — it was **the worst method tested**, because the robot committed to the place the prior named. In the real-world run (an 8 × 8 m pre-mapped kitchen/living room, Fetch robot) the best method reached **70 % success in 4.86 view poses, 103 s and 8.32 m of driving**, and *every failure was a false-negative detection, not a failed search*.

**Two numbers bound what having the map and the memory is worth.** The known-map advantage is cleanly isolated only once, by MultiON's oracle condition: an agent handed the full ground-truth map *including goal locations* succeeds **94 % (PPL 77)** against **62 % (PPL 35)** for the same agent with no map — and even the oracle falls to 48 % when three objects must be found in sequence. The memory advantage is measured best by GOAT on a real robot over 675 goals in 9 homes: ablating the object memory drops success **83.0 % → 60.3 %** and SPL **0.642 → 0.188**, and the memory-equipped system climbs from 60 % success at the first goal to 90 % by goals 5–10 while the memory-free version *"shows no improvement from experience"*. Note the sim/real split honestly: in GOAT-Bench's simulation, memory bought **efficiency (~1.5–1.9× SPL) but no success at all**. Also worth knowing for scoping: of the tidying systems, **TidyBot, TidyBot++, Mobile ALOHA and Housekeep all re-detect from scratch with no persistent object memory** — only OK-Robot re-finds from a stored map, at 58.5 % over 171 real trials, and its memory is *frozen after the initial scan*.

Two transfers, both of which the recommendation below encodes. First, **a last-known location is a noisy static prior, which is the configuration measured to be actively harmful** — it must carry a covariance that grows with time-since-seen, and the search must be willing to leave it. Second, **the binding failure mode in a pre-mapped room was perception, not navigation.** This rig drives to a commanded pose at 2.5 cm median; the hard half of `locate_object` is the looking, exactly as `docs/next-campaign-scoping.md` already suspects.

**The closest published system to this exact problem is "Where Did I Leave My Glasses?" (RA-L 2026)** — from the POCD authors, so it is the change-detection lineage grown into a search system. Each object carries pose, cloud, bbox, open-vocabulary class, **first-observation and disappearance times**, a geometric-change belief and a stationarity score, plus an **LLM-assigned semantic stationarity label** (static/dynamic) that injects synthetic change for unseen objects, so belief decays faster for things a language model expects to move. The mechanism worth copying is that **search regions are Gaussians whose variance is a function of the stationarity score** — less stationary means a wider search. Reported: 26.35 % higher change-detection F1 than geometric-only on the Khronos dataset, 95 % of map changes detected, and a 29 % efficiency gain over random or patrol search. DynaMem (2024) is the voxel-level cousin and reports **70 % success on non-stationary objects versus 30 % for a static baseline**, with a staleness-driven exploration value map.

The escalation itself — verify in place, re-observe from a few viewpoints, then widen — is the shape all of these take rather than one citable algorithm. The widening step is next-best-view selection, and this wiki already has that machinery in [[active-slam-exploration]] §3: score candidate poses by expected information gain, here over the belief about *one object* rather than over the map.

## 4. Verification: deciding "this is the same object"

Three families, in increasing cost and increasing discriminating power.

**Geometric association** is what the change-detection systems use, and the mechanism is consistent across them: a gated assignment (Hungarian or JCBB) over a cost that mixes centroid distance, orientation and class agreement, with a hard gate so an implausible match is simply refused. POCD's cost is point-to-plane ICP residual plus distance² plus angle plus class mismatch; the reported operating point is 80.2 % precision at 3.0 % false-positive rate on a warehouse dataset ([[dynamic-object-handling]]). The gate is the load-bearing part: an ungated assignment will always produce a match, because assignment problems always have a solution. Fusion++'s gate is worth quoting exactly because it is widely misreported as an IoU: association is **intersection over the *detection's* area > 0.2**, while a separate **3-D bounding-box IoU < 0.5** is what gates *spawning a new object* — two different thresholds doing two different jobs, and the rig needs both (one to match, one to decide something new has appeared). **The gates in actual use are loose, and that is deliberate**: nuScenes tracking abandoned IoU for a plain **2 m centre distance** because small objects with a small translation error score zero IoU; Panoptic Multi-TSDFs requires only **IoU ≥ 0.1** plus a matching class; CubeSLAM rejects box overlap outright in favour of *shared feature count > 10* precisely because overlap fails on repeated objects; and AB3DMOT's ablation shows **tightening the gate monotonically hurts** (IoU_min 0.01 → 0.25 takes ID switches from 0 to 19). For a 5 cm footprint map the lesson transfers directly — prefer a generous centre-distance gate over a tight overlap test, and spend the discrimination budget on the *shape* and *permanence* channels instead.

**Appearance re-ID** is the camera route, and [[object-fingerprint-memory]] settles the mechanics (DINOv2 multi-view max-cosine, precision-favoured threshold, margin as confidence) — but the CUTE benchmark's numbers are much more sobering than that page implies and should be read before anyone treats the camera as the answer. On objects re-photographed in a different scene, **raw DINOv2 scores 47.3 % top-1 against a chance floor of ~43–50 %** — a coin flip; cropping the background out first is what rescues it to 69.0 %. On the discriminating case that matters here, telling apart two *visually similar instances of the same category*, DINOv2 scores **0.42 (familiar) / 0.32 (unfamiliar) against a human 0.90 / 0.80**. The one strongly positive result is InsDet, where SAM + off-the-shelf DINOv2 beats trained detectors (41.6 vs 28.0 AP) — but even there occlusion costs 41 % relative and small objects 79 %. **Two similar chairs in two rooms are close to a coin flip for the appearance channel alone.**

**The failure modes are the reason both are needed.** A second identical chair defeats appearance but not geometry (two records at two places). An object moved 20 cm defeats geometry but not appearance. Occlusion defeats both, and is the case that must never be scored as a negative — see §6. The general rule visible across POCD, Fusion++ and the DovSG update is: **never let one channel alone promote a match.** A strong appearance match at an implausible distance is `uncertain`, not a teleport — [[world-model-architecture]] already states this rule; what this page adds is that on the rover today there *is* no appearance channel, so the geometry gate is carrying the whole load and must be correspondingly conservative.

## 5. What "identified" can mean on a geometry-only sensor

This is the part with no comfortable literature, and the rig-specific content matters more than the citations.

**A single 1.1 m plane gives a 2-D cross-section and nothing else.** From it one can honestly recover: a footprint's position, its extent along the slice, its rough shape class (straight run / corner / curve / thin isolated return), and its persistence across maps. One cannot recover height, volume, colour, material, upright-vs-tipped, or anything that distinguishes two objects with similar cross-sections at that one height. EDA226 states the same thing from the data side: *"the desk is 'a block' because that is what a slice of a desk looks like."* The published 2-D-range recognisers do exist — DROW (464k scans, 24k annotated) and its successors DR-SPAAM and *Deep Person Detection in 2D Range Data* — but note what they are: **single-purpose detectors for people and mobility aids, trained on tens of thousands of hand-annotated instances, and topping out at peak-F1 ≈ 68 % for the person class.** The classical Arras feature set re-trained on the same data manages peak-F1 50.3 %. And there appears to be **no published work classifying general household furniture from a single plane at all** — the nearest solved task is *place* classification (room / corridor / doorway at 91.2 %, and going from three classes to six required adding a **camera**).

**The mounting height is the part that should settle the argument.** Every verified single-plane detector sits at **30–40 cm** — Arras 30, Leigh ~30, DROW 37, JRDB at "lower legs" — because that is the narrowest, most convex slice of a person. Two independent papers state that **80 cm is already too high**: DROW's authors note their 80 cm sensor is "where individual legs are usually not visible", and Leigh's that 0.8 m "is above the leg-region of many pedestrians". **Nothing in the literature operates at 1.1 m.** The price of the plane is quantified once, by the JRDB 2D-visible ablation: restricted to targets that actually leave ≥ 5 points in the plane, DR-SPAAM scores 77.2 AP; over all targets it scores **47.6** — roughly **38 % of achievable performance lost purely to things that leave no trace in the plane**. That is the honest prior for a 1.1 m slice of a room full of desks and boxes.

So for the first stage, **"identified" should mean a persistent handle, not a name** — which is the honest answer to the scoping doc's open question 4. The object is the same object if it is at the expected place with a compatible footprint and a compatible permanence class. That is a weak test, and it should be reported as one: `found(confidence)`, not `found`.

**What multi-height tilt sweeps add — and, honestly, what they do not.** Several planes turn a cross-section into a coarse vertical profile, which separates a desk from a chair from a box from a wall by *shape at scale*. Zheng et al.'s multi-object-search experiments support the need from the planning side: their search regions are explicitly 2 m tall because a single height does not contain the objects. But the evidence says tilt buys **coverage and geometry, not semantics** — LOAM, the canonical nodding 2-D LiDAR, bought ~1 % indoor drift and good maps, never object identity; and there is **no published study of recognition accuracy versus one, two or four scan planes** (every beam ablation in the field starts at 8 and is automotive). So tilt should be justified as a *coverage and change-detection* lever — it makes "should I have seen it" answerable at more than one height — rather than as a route to naming things. Two cautions from `docs/next-campaign-scoping.md`, both real: a tilted plane breaks the 2-D assumption every current registration makes, and the guard shares the scan plane, so a sweep is a stationary-only operation.

**Where the camera becomes necessary rather than optional** is the moment the question changes from "is something there" to "is it *my* thing". Two same-category instances, a named object, a mug versus a different mug — none of these is reachable from geometry at any number of heights. The camera has been on the rig throughout and has never been used on the rover; June's hand-held hloc relocalised at 96 % and objmem's DINOv2 loop runs, so the pieces exist but the integration is unmeasured. The defensible sequencing (scoping open question 2) is **geometry-only first, because it is the only channel proven on this rover, and add the camera at the verification step** — which is also where SLiM's real-world failures were.

## 6. Change detection: moved, occluded, or gone

The distinction that makes this tractable is **negative evidence**: a cell or object that was *not looked at* is not evidence of absence. EDA226 already implements exactly this and should be recognised as the rig's change-detection substrate rather than a one-off analysis — visibility is computed per map ("a cell is 'looked at' if one of that map's station rays passed through or ended in it"), and the three-way permanent / semi-permanent / unverified split is a direct instance of the [[robust-evidence-mapping-principle]]. The published systems agree on the mechanism, and **Khronos is the one that solves it properly** — worth studying before building this, because it is the design the rig should imitate. Its object node stores `first_observed_ns` / `last_observed_ns` as **vectors — a sequence of presence intervals, not a single pair** — and because positive observations alone cannot separate *gone* from *not looked at*, it maintains a **library of free-space rays** from background points to the observing robot pose, spatially hashed so both endpoints survive loop closure. A timestamp counts as evidence of absence only when **≥ 60 % of rays within a 5 s window** mark the object absent; the disappearance time is then set to the midpoint of the ambiguous window. Two honest limitations to carry with it: Khronos stores no fragment descriptor, so **it detects that an object went away but cannot re-associate it with the one that appeared elsewhere** (exactly the gap an appearance channel fills), and its **change-detection precision is only 21–44 % in simulation** — most "this changed" events are wrong even in the 2024 state of the art. **POCD is the anti-pattern to avoid**: its visibility test is *pure frustum containment*, so an object inside the frustum but occluded by nearer furniture receives the same maximum pseudo-change as one that is genuinely gone and is pushed toward deletion. In a cluttered room that deletes things standing behind other things. If a TSDF is ever kept, Fehr et al.'s trick is free — voxel weight `w = 0` means unobserved, `w > 0 ∧ f = t` means observed-free — and Panoptic Multi-TSDFs makes the tri-state {persistent, unobserved, absent} explicit and *reversible* on re-observation.

The practical rules, all implementable on the rig today:

- **Ray-cast the stored footprint from the current station.** If the rays pass *through* where it was, that is observed absence. If they stop short, that is occlusion — and the occluder is itself a useful record. DynaMem does exactly this at voxel level (a voxel in the frustum and not occluded by the current depth is deleted); Khronos's 60 %-of-rays gate is the fraction worth starting from rather than requiring unanimity.
- **Never commit `removed` on one miss.** [[world-model-architecture]] already sets N-consecutive-misses-from-viewpoints-that-should-have-seen-it; the rig's addition is that "should have seen it" is computable from the EDA226 visibility test rather than guessed — and that three maps over five days is **not** a mobility prior, as EDA226 says itself. Until there are runs across days with deliberate rearrangement, `permanent` means "unmoved in the observations we have", and `locate_object` should treat the class as a *search-width* parameter, not a truth claim.
- **`τ_move` has a measured floor here**, which is unusually good news: relocalisation with a prior is 0.5 cm / 0.4° median (99 % within 3 cm / 2°), and cross-map registration is ~2 cm rms, so motion above ~5 cm is detectable — far better than the ~⅓ m floor the June camera-based design had to assume. EDA226's cell-exact-versus-one-cell-tolerance experiment (1119 semi-permanent cells collapsing to 549) is the direct measurement of the artefact that sets this floor at 5 cm resolution.

---

## Recommendation for this rig — the `locate_object(handle)` protocol

1. **Store the record beside the locked map, never in it.** One row per object instance in a small store keyed to the office map frame: `handle`, `footprint` (the 2-D points and their fitted extent, not a centroid alone), `position` + covariance, `permanence ∈ {permanent, semi-permanent, unverified}` from the EDA226 cross-map test, `last_seen_ts`, `n_observations`, `state ∈ {present, moved, gone, uncertain}`, and an append-only history. Nothing here may modify `m1.json` or the station poses.
2. **Add the field the classical schemas omit: `last_seen_from`** — the rover pose, tilt angle, range and incidence at which the observation was made, for each of the last few sightings, alongside a *sequence* of presence intervals rather than one `last_seen_ts` (the Khronos form). This is what step 4 drives to, it is what STAR and EmbodiedLGR converged on, and it costs nothing.
3. **Grow the position covariance with time-since-seen, scaled by `permanence`.** A `permanent` object's covariance grows slowly; a `semi-permanent` one's grows fast. This is the SLiM correction — the prior must be dynamic, because a confident stale prior measured *worse than no prior at all* — and it has two published forms to copy: the **persistence filter**'s constant-time Bayesian decay from last-observation time, and "Where Did I Leave My Glasses?"'s rule that **search-region variance is a function of the stationarity score**. The rig's `permanence` class is the input both of them want.
4. **Verify in place first.** Plan to the recorded `last_seen_from` pose (not to the object's coordinates — reproduce the observation, and keep the standoff that worked), stop, relocalise from the odometry prior, capture, clean, and test for a footprint match within the current covariance. Accept only a `strong` unambiguous basin; refuse to conclude anything from a bad relocalisation.
5. **Score the match on three channels, gated.** Centroid distance within the covariance gate; footprint extent and shape compatible; permanence class compatible. Report `found(confidence)` with the margin to the best rival object as the confidence, and never promote a match that fails the distance gate however good the shape agreement.
6. **If not found in place, re-observe from 2–3 viewpoints before believing it.** Choose them for *parallax and incidence* on the stored footprint — different faces, incidence ≤ 70° — inside the drivable band. In a 1.9 × 1.2 m band that is a small, enumerable set; pick it by the observability rule the mapper already uses, not by a spacing heuristic.
7. **Ray-cast before declaring anything absent.** If the stored footprint's cells were passed *through* by this station's rays, that is observed absence → `moved`. If rays stopped short, that is occlusion → `uncertain`, and record the occluder. Absence from a cell nobody looked at is not evidence.
8. **Escalate to a widening search only on an explicit flag, and bound it.** `locate_object(handle, search=True)` searches the drivable band by expected information gain over the object's belief ([[active-slam-exploration]] §3), ordered nearest-first from the last-known position, with a hard budget — 4–6 stations or one battery run, whichever comes first. Default is `search=False`: report `moved` and stop. This is the honest answer to scoping open question 3, and it keeps the first demo inside one charge.
9. **Report a four-way outcome, never a boolean:** `found(confidence, pose)` · `moved(new_pose)` if re-found elsewhere · `absent(searched_region)` if the region was searched and the rays saw through · `uncertain(reason)` for occlusion, bad relocalisation, or budget exhausted. Write every outcome to the object's history with the station pose it was judged from.
10. **Demo it geometry-only, then add the camera at step 5.** A persistent handle re-found across two runs is a complete, honest end-to-end demonstration of the object layer. The camera — hloc for the pose it is already good at, DINOv2 for the identity the LiDAR cannot supply — is the upgrade that turns `handle` into `name`, and its first job should be the verification step, because that is where the reference system's real-world failures were.
11. **Before quoting any number:** ≥ 10 `locate_object` calls over ≥ 2 objects and ≥ 2 sessions, including at least one object deliberately moved and one deliberately occluded, with the outcome and the ground truth recorded by hand each time.

## Sources

*(all accessed 2026-09-22)*

- [slam++] Salas-Moreno, Newcombe, Strasdat, Kelly, Davison, "SLAM++: Simultaneous Localisation and Mapping at the Level of Objects," CVPR 2013. doi:10.1109/CVPR.2013.178 — object nodes in a pose graph, recognised against a **pre-scanned database of 3-D models**.
- [fusion++] McCormac, Clark, Bloesch, Davison, Leutenegger, "Fusion++: Volumetric Object-Level SLAM," 3DV 2018. arXiv:1808.08378 — per-object TSDF at object-dependent resolution, 3-D foreground mask, 6-DoF pose graph as the *only* persistent map, semantics refined over time, **existence probability**. https://arxiv.org/abs/1808.08378
- [quadricslam] Nicholson, Milford, Sünderhauf, "QuadricSLAM: Dual Quadrics from Object Detections as Landmarks in Object-oriented SLAM," IEEE RA-L 2019. arXiv:1804.04011 — a 9-DoF dual quadric encodes size, position and orientation, and data association is explicitly *assumed solved*. **CubeSLAM** (Yang & Scherer, T-RO 2019, arXiv:1806.00557) uses 9-DoF cuboids and rejects box-overlap association outright, matching on shared feature count > 10 because overlap "is not robust if there is severe object occlusion with repeated objects". https://arxiv.org/abs/1804.04011
- [hydra] Hughes, Chang, Hu, Talak, Abdulhai, Strader, Carlone, "Foundations of Spatial Perception for Robotics: Hierarchical Representations and Real-time Systems" (the Hydra journal version), IJRR 2024. arXiv:2305.07154 — object nodes are closed-set (centroid, bounding box, semantic label) with no language query path; room *labelling* is only 26.7–48.0 % accurate. **Clio** (RA-L 2024, arXiv:2404.13696) makes object granularity task-dependent via an Information Bottleneck, and in its Spot trial *"was only unable to select the correct target object in the scene graph once"* out of 21 — the 57 % end-to-end figure is manipulation and navigation failing, not retrieval. https://arxiv.org/abs/2305.07154
- [khronos] Schmid, Abate, Chang, Carlone, "Khronos: A Unified Approach for Spatio-Temporal Metric-Semantic SLAM in Dynamic Environments," RSS 2024. arXiv:2402.13817 — factorises fast short-term dynamics (active temporal window) from slow long-term change (factor graph). https://arxiv.org/abs/2402.13817
- [pocd] Qian, Chatrath, Yang, Servos, Schoellig, Waslander, "POCD: Probabilistic Object-Level Change Detection and Volumetric Mapping in Semi-Static Scenes," RSS 2022. arXiv:2205.01202 — per-object stationarity score + TSDF change measure, Bayesian update over geometry and semantics. https://arxiv.org/abs/2205.01202
- [vision-msgs] `ros-perception/vision_msgs`, `vision_msgs/msg/{Detection3D,ObjectHypothesisWithPose,BoundingBox3D}.msg` — fields read directly from the repository: header, `ObjectHypothesisWithPose[] results` (class id + score + `geometry_msgs/PoseWithCovariance`), `BoundingBox3D bbox` (centre pose + size, 6-DoF), and `string id` "used for consistency across multiple detection messages … may be empty". No appearance descriptor, no last-seen viewpoint, no permanence prior. https://github.com/ros-perception/vision_msgs
- [slim] Zeng, Röfer, Jenkins, "Semantic Linking Maps for Active Visual Object Search," ICRA 2020 (Best Paper Award in Cognitive Robotics). arXiv:2006.10807 — Table I (6 trials per target per method): searching for *cup*, informed-direct-search with a **known static** noisy prior scored **0.33** success vs **0.83** uninformed, **0.83** known-dynamic and **1.00** hybrid; real-world 8 × 8 m pre-mapped kitchen/living room on a Fetch, **0.70 success, 4.86 view poses, 103 s, 8.32 m**, all failures from false-negative detection. https://arxiv.org/abs/2006.10807
- [search-classics] Aydemir, Pronobis, Göbelbecker, Jensfelt, "Active Visual Object Search in Unknown Environments Using Uncertain Semantics," IEEE T-RO 29(4):986–1002, 2013, doi:10.1109/TRO.2013.2256686 (informed search cuts average time 36.4 → 15.8 min on an unknown map, 28.5 → 7.8 known; note the paper reports **no success rate** — failed runs are excluded); the direct-vs-indirect distinction originates with Garvey, "Perceptual Strategies for Purposive Vision," SRI Technical Note 117, 1976. [mos-pomdp] Zheng, Sung, Konidaris, Tellex, "Multi-Resolution POMDP Planning for Multi-Object Search in 3D," IROS 2021. arXiv:2005.02878 — octree belief, frustum field of view, robot experiments in two 10 m² × 2 m regions with objects at varying heights (3 of 6 objects found in ~15 min). Same group: **SLOOP** (arXiv:2012.02705) folds spatial language into the same OO-POMDP. https://arxiv.org/abs/2005.02878
- [rio] Wald, Avetisyan, Navab, Tombari, Nießner, "RIO: 3D Object Instance Re-Localization in Changing Indoor Environments" (the 3RScan dataset), ICCV 2019 (oral). arXiv:1908.06109 — 1482 RGB-D scans of 478 environments across time steps; task = recover a known instance's 6-DoF pose in a later scan. https://arxiv.org/abs/1908.06109
- [goat-bench] Khanna, Ramrakhya, Chhablani, Yenamandra, Gervet, Chang, Kira, Chaplot, Batra, Mottaghi, "GOAT-Bench: A Benchmark for Multi-Modal Lifelong Navigation," arXiv:2404.06609 — category / language / image goals, category / language / image goals, explicitly studying memory in lifelong scenarios. Companion benchmarks assuming no prior map: **Instance-ImageNav** (Krantz, Lee, Malik, Batra, Chaplot, arXiv:2211.15876 — goal image focused on one instance) and object-goal navigation, where a 2024 method reports **39.0 % SR** (Semantic Environment Atlas, arXiv:2410.09081). https://arxiv.org/abs/2404.06609
- [cute] Kotar, Tian, Yu, Yamins, Wu, "Are These the Same Apple? Comparing Images Based on Object Intrinsics," NeurIPS 2023 Datasets & Benchmarks. arXiv:2311.00750 — CUTE: 18,000 images of 180 objects under varying lighting, pose and imaging conditions; **LPIPS and CLIP scores do not measure object intrinsics well**; contrastive self-supervised features with foreground filtering is the strong baseline. https://arxiv.org/abs/2311.00750
- [glasses] Bogenberger, Qian, Schoellig et al., "Where Did I Leave My Glasses? Open-Vocabulary Object-Level Change Detection and Search in Semi-Static Scenes," IEEE RA-L 2026. arXiv:2509.19851 — per-object pose, cloud, bbox, CLIP class, first-observation and disappearance times, geometric-change belief, stationarity score and an **LLM-assigned semantic stationarity label**; **search-region variance is a function of the stationarity score**; +26.35 % change-detection F1 over geometric-only on the Khronos dataset, 95 % of map changes detected, 29 % search-efficiency gain over random/patrol. The closest published system to this page's problem. https://arxiv.org/abs/2509.19851
- [star] Chen, Kumar, Xu, Pavlakos, Biswas, Martín-Martín, "STAR: Searching in Space and Time," arXiv:2511.14004 — long-term memory record `(t, robot pose, embedding, raw observation)` with temporal, **spatial (indexed by robot pose)** and semantic indices; visible-object search 65–78 % with an oracle, 56–100 % across 54 real Tiago tasks. **EmbodiedLGR** (IROS 2026, arXiv:2604.18271) stores the same triplet — embedded label, last-associated pose, last-observed time — with honest numbers: 29.17 % positional / 38.63 % temporal query accuracy. https://arxiv.org/abs/2511.14004
- [persistence-filter] Rosen, Mason, Leonard, "Towards Lifelong Feature-Based Mapping in Semi-Static Environments," ICRA 2016. doi:10.1109/ICRA.2016.7487237 — the canonical last-observation-time decay: exact constant-time recursive Bayesian belief in feature persistence under a survival-time prior, parameterised through its hazard function and detector error rates `(P_M, P_F)`. Evaluated only on synthetic sequences from its own generative model — the paper has no results tables. **Perpetua** (IROS 2025, arXiv:2507.18808) adds *emergence* filters for things that come back (UFPR04 parking lot, 12,427 images over 30+ days, F1 0.745). https://david-m-rosen.github.io/publication/persistencefilter-icra/PersistenceFilter-ICRA.pdf
- [dynamem] "DynaMem: Online Dynamic Spatio-Semantic Memory for Open World Mobile Manipulation," arXiv:2411.04999 — per-voxel `{location, observation count, source image id, semantic feature, latest observation time}`; staleness-driven exploration value map; removal by ray-casting (a voxel in the frustum and unoccluded by current depth is deleted); **70 % success on non-stationary objects vs 30 % static baseline**. https://arxiv.org/abs/2411.04999
- [multion] Wani, Patel, Jain, Chang, Savva, "MultiON: Benchmarking Semantic Map Memory using Multi-Object Navigation," NeurIPS 2020. arXiv:2012.03912 — the one clean isolation of the known-map advantage: **OracleMap 94 % success / PPL 77 vs NoMap 62 % / 35** on one goal, falling to 48 % for three goals even with a perfect map. https://arxiv.org/abs/2012.03912
- [goat] Chang, Gervet, Khanna et al., "GOAT: GO to Any Thing," arXiv:2311.06430 — 9 homes, 675 goals, >90 h. Memory ablation **83.0 % → 60.3 % success, SPL 0.642 → 0.188**; with memory, success climbs 60 % → 90 % from goal 1 to goals 5–10, while the memory-free ablation shows no improvement from experience. Contrast GOAT-Bench (sim), where memory buys ~1.5–1.9× SPL and no success. https://arxiv.org/abs/2311.06430
- [cos-pomdp] Zheng, Chitnis, Liu, Tellex, "Towards Optimal Correlational Object Search," ICRA 2022. arXiv:2110.09991 — second independent confirmation that a bad prior is worse than none: *estimated* correlations score SPL 14.23, *reversed* correlations 16.76. https://arxiv.org/abs/2110.09991
- [ok-robot] Liu, Ha, Shah, Mo, Pinto et al., "OK-Robot: What Really Matters in Integrating Open-Knowledge Models for Robotics," arXiv:2401.12202 — the only TidyBot-class system that re-finds from a persistent memory: **58.5 % over 171 trials in 10 homes**, memory frozen after the first scan, retrieval the top non-manipulation failure (9.3 %). Contrast Housekeep (arXiv:2205.10712), which wipes its map every episode and finishes **~1 % of episodes** even with ground-truth segmentation. https://arxiv.org/abs/2401.12202
- [insdet] Shen, Guo, Ramanan et al., "A High-Resolution Dataset for Instance Detection with Multi-View Instance Capture," NeurIPS 2023 D&B. arXiv:2310.19257 — SAM + off-the-shelf DINOv2 **41.6 AP** vs end-to-end DINO **28.0**; occlusion costs 41 % relative (28.0 hard vs 47.6 easy), small size 79 % (14.6 vs 69.1). https://arxiv.org/abs/2310.19257
- [gates] Association-gate values in practice: nuScenes tracking uses a **2 m centre distance** rather than IoU, because "objects with small footprints … if detected with a small translation error, give 0 IOU" (nuScenes devkit tracking README); Panoptic Multi-TSDFs requires **IoU ≥ 0.1** plus class agreement; AB3DMOT (arXiv:1907.03961) reports that raising IoU_min from 0.01 to 0.25 takes ID switches from **0 to 19** and causes "a consistent drop in all metrics". https://github.com/nutonomy/nuscenes-devkit
- [fehr] Fehr, Furrer, Dryanovski, Sturm, Gilitschenski, Siegwart, Cadena, "TSDF-based Change Detection for Consistent Long-Term Dense Reconstruction and Dynamic Object Discovery," ICRA 2017. doi:10.1109/ICRA.2017.7989614 — the origin of the negative-evidence argument: a TSDF "already models free space (`f = t ∧ w > 0`) and unobserved space (`w = 0`)", solving occlusion "without any viewpoint dependent data". Caveat: only 23 scans of 3 scenes, partly tripod-mounted, with heavy over-segmentation. **Panoptic Multi-TSDFs** (Schmid, Delmerico, Schönberger, Nieto, Pollefeys, Siegwart, Cadena, ICRA 2022, arXiv:2109.10165) makes the tri-state {persistent, unobserved, absent} explicit and reversible, at object-granularity submaps; code at github.com/ethz-asl/panoptic_mapping.
- [2d-vs-3d] Jia, Hermans, Leibe, "Domain Transfer for Semantic Segmentation of LiDAR Data using Deep Neural Networks" / "2D vs. 3D LiDAR-based Person Detection on Mobile Robots," IROS 2022. arXiv:2106.11239 — the price of a single scan plane: DR-SPAAM scores **77.2 AP on targets visible in the plane but 47.6 overall**; "it is easy for persons to be fully occluded, and thus impossible to detect, from the scan plane of 2D LiDAR sensors". https://arxiv.org/abs/2106.11239
- [2d-heights] Mounting heights and ceilings, all verified in-paper: Arras, Mozos, Burgard (ICRA 2007) 30 cm, 97.25 % overall on a 2-class problem; Leigh et al. (ICRA 2015) ~30 cm, **MOTA 33.2 % stationary / 10.2 % moving**; DROW v1/v2 (arXiv:1603.02636 — 464k scans, 24k annotated; arXiv:1804.02463) 37 cm, **person peak-F1 68.1 %**, Arras features re-trained 50.3 %; DR-SPAAM (arXiv:2004.14079) AP 70.3, 22.6 FPS on a Jetson AGX. DROW v2 §II and Leigh §V-A both state that **80 cm is already above the leg region**. Mozos et al., *RAS* 55(5):391–402, 2007 — place classification {room, corridor, doorway} 91.2 % from one plane, but six classes **required camera features**. doi:10.1016/j.robot.2006.12.003
- [loam] Zhang, Singh, "LOAM: Lidar Odometry and Mapping in Real-time," RSS 2014 — a nodding 2-D Hokuyo; ~1 % relative error indoors, 2.5 % outdoors. Extra planes bought pose and geometry, not semantics; no published study compares recognition accuracy at 1 vs 2 vs 4 planes.
- [rep-155] ROS REP-155, "ROS4HRI: Human Representation for Human-Robot Interaction" — the one ROS standard specifying *persistent* identity: a permanent person id that MAY be serialised across reboots, a `/humans/persons/known` list, a frame that "might be located to the **last known position**", and a `/location_confidence` that decays to "location unknown". Standardised for people, never for objects. https://ros.org/reps/rep-0155.html
