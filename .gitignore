# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
*.egg-info/

# Playwright
.playwright-mcp/
playwright/.cache/

# Obsidian (user-specific state)
.obsidian/
wiki/.obsidian/

# Raw captures (markdown renderings + PNG assets + .ingest summaries).
# All derivable from the arxiv URL in each wiki page's `## Source` section;
# committing them bloats the repo with staging artifacts. The wiki/ pages are
# the committed product. Re-run `capture_pdf` if you need raw text again.
raw/research/

# OS
.DS_Store
Thumbs.db
